// [逐行教學 HEADER] ===============================================================
// [逐行教學 HEADER] 檔案：pedestrian_rom_red.v
// [逐行教學 HEADER] 主要模組：pedestrian_rom_red
// [逐行教學 HEADER] 整體用途：紅色行人停止圖樣 ROM。
// [逐行教學 HEADER] 每一個「原始實體行」前都加入 [語法] + [用途]，原始程式行本身不修改。
// [逐行教學 HEADER] 若一個原始行塞了多個敘述，註解會把同一行內的宣告/賦值/子模組一起拆解說明。
// [逐行教學 HEADER] ===============================================================
// [逐行教學 L001][語法] `module` 宣告一個硬體模組；模組名稱後括號列出對外 I/O；埠列表直接出現 `input/output`，屬 ANSI-style 埠宣告；`[msb:lsb]`/`[index]`
// [逐行教學 L001][語法] 用來選取向量的一段或單一 bit。
// [逐行教學 L001][用途] 紅色行人停止圖樣 ROM。。
module pedestrian_rom_red(input [2:0] row,output reg [7:0] data);
// [逐行教學 L002][語法] `always @(*)` 自動把區塊內讀到的訊號加入敏感度表，通常描述組合邏輯；同一行的 `begin` 開始一個多敘述區塊；`case(row)` 是多路選擇語法，依選擇值匹配各個
// [逐行教學 L002][語法] `標籤:`；`endcase` 結束前面的 case 多路選擇區塊；`default:` 是 case 沒有其他標籤匹配時使用的預設分支；`data = ...`
// [逐行教學 L002][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`8'h0A` = 8-bit 十六進位常數；`8'h04` = 8-bit 十六進位常數；`8'h04` = 8-bit
// [逐行教學 L002][語法] 十六進位常數；`8'h1F` = 8-bit 十六進位常數。
// [逐行教學 L002][用途] 任一輸入改變就重新計算本區塊；所有路徑都有賦值時不需要記憶狀態；硬體上通常形成解碼器/MUX，用狀態、按鍵或索引選擇不同輸出；完成目前 case 解碼邏輯；提供未列舉情況的安全輸出/預設動作；更新
// [逐行教學 L002][用途] `data`（輸出資料），下一值由 `8'h0A` 計算；更新 `data`（輸出資料），下一值由 `8'h04` 計算；更新 `data`（輸出資料），下一值由 `8'h1F` 計算；更新
// [逐行教學 L002][用途] `data`（輸出資料），下一值由 `8'h0E` 計算；更新 `data`（輸出資料）為 0/關閉或初始值。
 always @(*) begin case(row) 0:data=8'h0A;1:data=8'h04;2:data=8'h04;3:data=8'h1F;4:data=8'h04;5:data=8'h0E;6:data=8'h0E;7:data=8'h0E;default:data=0;endcase end
// [逐行教學 L003][語法] `endmodule` 與前面的 `module` 配對，結束模組定義。
// [逐行教學 L003][用途] 結束 `pedestrian_rom_red` 的 RTL 描述。
endmodule
