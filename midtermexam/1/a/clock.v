// [逐行教學 HEADER] ===============================================================
// [逐行教學 HEADER] 檔案：clock.v
// [逐行教學 HEADER] 主要模組：clock
// [逐行教學 HEADER] 整體用途：計時/倒數核心：依時脈更新數字資料。
// [逐行教學 HEADER] 每一個「原始實體行」前都加入 [語法] + [用途]，原始程式行本身不修改。
// [逐行教學 HEADER] 若一個原始行塞了多個敘述，註解會把同一行內的宣告/賦值/子模組一起拆解說明。
// [逐行教學 HEADER] ===============================================================
// [逐行教學 L001][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L001][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
// Q1(a): 12-hour countdown. Reset loads 12:00:00; stops at 00:00:00.
// [逐行教學 L002][語法] `module` 宣告一個硬體模組；模組名稱後括號列出對外 I/O。
// [逐行教學 L002][用途] 計時/倒數核心：依時脈更新數字資料。。
module clock(
// [逐行教學 L003][語法] `input` 宣告由外部進入模組的輸入埠。
// [逐行教學 L003][用途] 本段宣告：clk=系統時脈；reset=重置。
    input clk, input reset, input tick_1hz, input enable,
// [逐行教學 L004][語法] `output` 宣告由模組送到外部的輸出埠；`[3:0]` 指定位元範圍；`output reg` 允許此輸出在 always 程序區塊被賦值。
// [逐行教學 L004][用途] 本段宣告：count5, count4, count3, count2, count1, count0。
    output reg [3:0] count5, count4, count3, count2, count1, count0,
// [逐行教學 L005][語法] `output` 宣告由模組送到外部的輸出埠。
// [逐行教學 L005][用途] 本段宣告：done。
    output done
// [逐行教學 L006][語法] 這是一行 Verilog RTL 敘述；它延續目前區塊的宣告、控制或資料處理。
// [逐行教學 L006][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
);
// [逐行教學 L007][語法] `assign 左式 = 右式;` 是 continuous assignment；這裡的 `=` 屬連續指定，不是 always 內的阻塞賦值；`==` 是相等比較，結果為 1 或 0；`&&`
// [逐行教學 L007][語法] 是邏輯 AND：兩個條件都成立才為真。
// [逐行教學 L007][用途] 更新 `done`，下一值由 `(count5==0 && count4==0 && count3==0 && count2==0 && count1==0 && count0==0)`
// [逐行教學 L007][用途] 計算；右式一變化就立即重新驅動左側 wire。
    assign done = (count5==0 && count4==0 && count3==0 && count2==0 && count1==0 && count0==0);
// [逐行教學 L008][語法] `always @(posedge/negedge ...)` 是邊緣觸發的時序程序區塊；監看 clk 的上升緣、reset 的上升緣；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L008][用途] 通常綜合成 DFF/暫存器，在指定邊緣更新狀態；Reset 也在敏感度表，因此是非同步重置。
    always @(posedge clk or posedge reset) begin
// [逐行教學 L009][語法] `if (reset)` 是條件分支：條件為 1 才執行後續敘述。
// [逐行教學 L009][用途] 檢查 `reset` 以決定資料/狀態要走哪一條路徑；這是重置分支。
        if (reset) begin
// [逐行教學 L010][語法] `count5 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`count4 <= ...`
// [逐行教學 L010][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`count3 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`count2 <= ...`
// [逐行教學 L010][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`count1 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`count0 <= ...`
// [逐行教學 L010][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`4'd1` = 4-bit 十進位常數；`4'd2` = 4-bit 十進位常數；`4'd0` = 4-bit
// [逐行教學 L010][語法] 十進位常數；`4'd0` = 4-bit 十進位常數。
// [逐行教學 L010][用途] 更新 `count5`，下一值由 `4'd1` 計算；更新 `count4`，下一值由 `4'd2` 計算；更新 `count3`，下一值由 `4'd0` 計算；更新 `count2`，下一值由
// [逐行教學 L010][用途] `4'd0` 計算；更新 `count1`，下一值由 `4'd0` 計算；更新 `count0`，下一值由 `4'd0` 計算。
            count5<=4'd1; count4<=4'd2; count3<=4'd0; count2<=4'd0; count1<=4'd0; count0<=4'd0;
// [逐行教學 L011][語法] `if (tick_1hz && enable && !done)` 是條件分支：條件為 1 才執行後續敘述；`else if` 只在前面 if 不成立時繼續測試下一個條件；`&&` 是邏輯
// [逐行教學 L011][語法] AND：兩個條件都成立才為真。
// [逐行教學 L011][用途] 檢查 `tick_1hz && enable && !done` 以決定資料/狀態要走哪一條路徑。
        end else if (tick_1hz && enable && !done) begin
// [逐行教學 L012][語法] `if (count0 != 0)` 是條件分支：條件為 1 才執行後續敘述；`count0 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` =
// [逐行教學 L012][語法] 1-bit 二進位常數；`!=` 是不相等比較。
// [逐行教學 L012][用途] 檢查 `count0 != 0` 以決定資料/狀態要走哪一條路徑；更新 `count0`為舊值 -1，完成向下計數。
            if (count0 != 0) count0 <= count0 - 1'b1;
// [逐行教學 L013][語法] `else` 是前面條件都不成立時執行的備援分支。
// [逐行教學 L013][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
            else begin
// [逐行教學 L014][語法] `count0 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`4'd9` = 4-bit 十進位常數。
// [逐行教學 L014][用途] 更新 `count0`，下一值由 `4'd9` 計算。
                count0 <= 4'd9;
// [逐行教學 L015][語法] `if (count1 != 0)` 是條件分支：條件為 1 才執行後續敘述；`count1 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` =
// [逐行教學 L015][語法] 1-bit 二進位常數；`!=` 是不相等比較。
// [逐行教學 L015][用途] 檢查 `count1 != 0` 以決定資料/狀態要走哪一條路徑；更新 `count1`為舊值 -1，完成向下計數。
                if (count1 != 0) count1 <= count1 - 1'b1;
// [逐行教學 L016][語法] `else` 是前面條件都不成立時執行的備援分支。
// [逐行教學 L016][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
                else begin
// [逐行教學 L017][語法] `count1 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`4'd5` = 4-bit 十進位常數。
// [逐行教學 L017][用途] 更新 `count1`，下一值由 `4'd5` 計算。
                    count1 <= 4'd5;
// [逐行教學 L018][語法] `if (count2 != 0)` 是條件分支：條件為 1 才執行後續敘述；`count2 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` =
// [逐行教學 L018][語法] 1-bit 二進位常數；`!=` 是不相等比較。
// [逐行教學 L018][用途] 檢查 `count2 != 0` 以決定資料/狀態要走哪一條路徑；更新 `count2`為舊值 -1，完成向下計數。
                    if (count2 != 0) count2 <= count2 - 1'b1;
// [逐行教學 L019][語法] `else` 是前面條件都不成立時執行的備援分支。
// [逐行教學 L019][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
                    else begin
// [逐行教學 L020][語法] `count2 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`4'd9` = 4-bit 十進位常數。
// [逐行教學 L020][用途] 更新 `count2`，下一值由 `4'd9` 計算。
                        count2 <= 4'd9;
// [逐行教學 L021][語法] `if (count3 != 0)` 是條件分支：條件為 1 才執行後續敘述；`count3 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` =
// [逐行教學 L021][語法] 1-bit 二進位常數；`!=` 是不相等比較。
// [逐行教學 L021][用途] 檢查 `count3 != 0` 以決定資料/狀態要走哪一條路徑；更新 `count3`為舊值 -1，完成向下計數。
                        if (count3 != 0) count3 <= count3 - 1'b1;
// [逐行教學 L022][語法] `else` 是前面條件都不成立時執行的備援分支。
// [逐行教學 L022][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
                        else begin
// [逐行教學 L023][語法] `count3 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`4'd5` = 4-bit 十進位常數。
// [逐行教學 L023][用途] 更新 `count3`，下一值由 `4'd5` 計算。
                            count3 <= 4'd5;
// [逐行教學 L024][語法] `if (count4 != 0)` 是條件分支：條件為 1 才執行後續敘述；`count4 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` =
// [逐行教學 L024][語法] 1-bit 二進位常數；`!=` 是不相等比較。
// [逐行教學 L024][用途] 檢查 `count4 != 0` 以決定資料/狀態要走哪一條路徑；更新 `count4`為舊值 -1，完成向下計數。
                            if (count4 != 0) count4 <= count4 - 1'b1;
// [逐行教學 L025][語法] `else` 是前面條件都不成立時執行的備援分支；`count4 <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`count5 <= ...`
// [逐行教學 L025][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`4'd9` = 4-bit 十進位常數；`1'b1` = 1-bit 二進位常數。
// [逐行教學 L025][用途] 更新 `count4`，下一值由 `4'd9` 計算；更新 `count5`為舊值 -1，完成向下計數。
                            else begin count4 <= 4'd9; count5 <= count5 - 1'b1; end
// [逐行教學 L026][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L026][用途] 關閉目前條件、時序或組合邏輯區塊。
                        end
// [逐行教學 L027][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L027][用途] 關閉目前條件、時序或組合邏輯區塊。
                    end
// [逐行教學 L028][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L028][用途] 關閉目前條件、時序或組合邏輯區塊。
                end
// [逐行教學 L029][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L029][用途] 關閉目前條件、時序或組合邏輯區塊。
            end
// [逐行教學 L030][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L030][用途] 關閉目前條件、時序或組合邏輯區塊。
        end
// [逐行教學 L031][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L031][用途] 關閉目前條件、時序或組合邏輯區塊。
    end
// [逐行教學 L032][語法] `endmodule` 與前面的 `module` 配對，結束模組定義。
// [逐行教學 L032][用途] 結束 `clock` 的 RTL 描述。
endmodule
