// [逐行教學 HEADER] ===============================================================
// [逐行教學 HEADER] 檔案：red_dot_top.v
// [逐行教學 HEADER] 主要模組：red_dot_top
// [逐行教學 HEADER] 整體用途：紅點迷宮頂層：整合鍵盤、移動、撞牆、倒數與 LED Matrix。
// [逐行教學 HEADER] 每一個「原始實體行」前都加入 [語法] + [用途]，原始程式行本身不修改。
// [逐行教學 HEADER] 若一個原始行塞了多個敘述，註解會把同一行內的宣告/賦值/子模組一起拆解說明。
// [逐行教學 HEADER] ===============================================================
// [逐行教學 L001][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L001][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
// TOP Q10 two-stage maze. 2/4/6/8 move. Portal 正 switches to stage 2.
// [逐行教學 L002][語法] `module` 宣告一個硬體模組；模組名稱後括號列出對外 I/O；埠列表直接出現 `input/output`，屬 ANSI-style 埠宣告；`[msb:lsb]`/`[index]`
// [逐行教學 L002][語法] 用來選取向量的一段或單一 bit。
// [逐行教學 L002][用途] 紅點迷宮頂層：整合鍵盤、移動、撞牆、倒數與 LED Matrix。。
module red_dot_top(input clk,input reset,input [2:0] column,
// [逐行教學 L003][語法] `output` 宣告由模組送到外部的輸出埠；`[7:0]` 指定位元範圍；`output reg` 允許此輸出在 always 程序區塊被賦值。
// [逐行教學 L003][用途] 本段宣告：red=紅色欄像素；green=綠色欄像素；row=LED Matrix 列選；sel=掃描/選擇訊號。
 output reg [7:0] red,output reg [7:0] green,output [7:0] row,output [2:0] sel,output [6:0] seg7);
// [逐行教學 L004][語法] `wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[3:0]` 指定位元範圍；`wire`
// [逐行教學 L004][語法] 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[2:0]` 指定位元範圍；`wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[7:0]` 指定位元範圍。
// [逐行教學 L004][用途] 本段宣告：key_event=有效按鍵事件；本段宣告：key_code=按鍵代碼；本段宣告：scan_row=目前掃描列索引；本段宣告：m1row, m2row, smile。
 wire clk_keyscan,clk_matrix,tick_keyscan,key_event; wire [3:0] key_code; wire [2:0] scan_row; wire [7:0] m1row,m2row,smile;
// [逐行教學 L005][語法] `reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定；`[1:0]` 指定位元範圍；`reg` 宣告可在
// [逐行教學 L005][語法] `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定；`reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由
// [逐行教學 L005][語法] always 類型決定；`[2:0]` 指定位元範圍。
// [逐行教學 L005][用途] 本段宣告：state=FSM 狀態；本段宣告：stage；本段宣告：pr, pc, nr, nc；本段宣告：move_req, outside。
 reg [1:0] state; reg stage; reg [2:0] pr,pc,nr,nc; reg move_req,outside;
// [逐行教學 L006][語法] `assign 左式 = 右式;` 是 continuous assignment；這裡的 `=` 屬連續指定，不是 always 內的阻塞賦值；`7'b0000000` = 7-bit 二進位常數。
// [逐行教學 L006][用途] 更新 `seg7`（七段段碼），下一值由 `7'b0000000` 計算；右式一變化就立即重新驅動左側 wire。
 assign seg7=7'b0000000;
// [逐行教學 L007][語法] `freq_div UKT(...)` 是子模組實例化；`UKT` 是這一顆硬體實例名稱；`#(14)` 是 parameter
// [逐行教學 L007][語法] override，用新值取代子模組預設參數；`.port(signal)` 是具名 port mapping；`freq_div UMT(...)` 是子模組實例化；`UMT`
// [逐行教學 L007][語法] 是這一顆硬體實例名稱；`#(10)` 是 parameter override，用新值取代子模組預設參數；`.port(signal)` 是具名 port mapping。
// [逐行教學 L007][用途] 時脈分頻器：利用二進位計數器的某一位，把 FPGA 高速系統時脈降成較慢時脈。。
 freq_div #(14) UKT(.clk_in(clk),.reset(reset),.clk_out(clk_keyscan)); freq_div #(10) UMT(.clk_in(clk),.reset(reset),.clk_out(clk_matrix));
// [逐行教學 L008][語法] `reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定；`assign 左式 = 右式;` 是 continuous
// [逐行教學 L008][語法] assignment；這裡的 `=` 屬連續指定，不是 always 內的阻塞賦值；單一 `&` 是逐位元 AND；`~` 是逐位元反相。
// [逐行教學 L008][用途] 本段宣告：clk_keyscan_d；更新 `tick_keyscan`，下一值由 `clk_keyscan & ~clk_keyscan_d` 計算；右式一變化就立即重新驅動左側 wire。
 reg clk_keyscan_d; assign tick_keyscan=clk_keyscan & ~clk_keyscan_d;
// [逐行教學 L009][語法] `always @(posedge/negedge ...)` 是邊緣觸發的時序程序區塊；監看 clk 的上升緣、reset 的上升緣；同一行的 `begin` 開始一個多敘述區塊；`if
// [逐行教學 L009][語法] (reset)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`clk_keyscan_d <= ...`
// [逐行教學 L009][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b0` = 1-bit 二進位常數。
// [逐行教學 L009][用途] 通常綜合成 DFF/暫存器，在指定邊緣更新狀態；Reset 也在敏感度表，因此是非同步重置；檢查 `reset` 以決定資料/狀態要走哪一條路徑；這是重置分支；更新 `clk_keyscan_d`為
// [逐行教學 L009][用途] 0/關閉或初始值；更新 `clk_keyscan_d`，下一值由 `clk_keyscan` 計算。
 always @(posedge clk or posedge reset) begin if(reset) clk_keyscan_d<=1'b0; else clk_keyscan_d<=clk_keyscan; end
// [逐行教學 L010][語法] `keypad_event UK(...)` 是子模組實例化；`UK` 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping。
// [逐行教學 L010][用途] 鍵盤事件模組：掃描鍵盤並產生單次 key_event/key_code。。
 keypad_event UK(.clk(clk),.reset(reset),.tick_scan(tick_keyscan),.column(column),.sel(sel),.key_event(key_event),.key_code(key_code));
// [逐行教學 L011][語法] `matrix_scan US(...)` 是子模組實例化；`US` 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping；`1'b1` = 1-bit 二進位常數。
// [逐行教學 L011][用途] 8×8 LED Matrix 掃描器。。
 matrix_scan US(.clk(clk_matrix),.reset(reset),.tick_scan(1'b1),.row_idx(scan_row),.row(row));
// [逐行教學 L012][語法] `map1 U1(...)` 是子模組實例化；`U1` 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping；`map2 U2(...)` 是子模組實例化；`U2`
// [逐行教學 L012][語法] 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping；`smile_rom U3(...)` 是子模組實例化；`U3`
// [逐行教學 L012][語法] 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping。
// [逐行教學 L012][用途] 第一關迷宮地圖 ROM。；第二關迷宮地圖 ROM。；過關笑臉 ROM。。
 map1 U1(.row_idx(scan_row),.data(m1row));map2 U2(.row_idx(scan_row),.data(m2row));smile_rom U3(.row_idx(scan_row),.data(smile));
// [逐行教學 L013][語法] `always @(*)` 自動把區塊內讀到的訊號加入敏感度表，通常描述組合邏輯；同一行的 `begin` 開始一個多敘述區塊；`if (key_event)` 是條件分支：條件為 1
// [逐行教學 L013][語法] 才執行後續敘述；`case(key_code)` 是多路選擇語法，依選擇值匹配各個 `標籤:`；`nr = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`nc
// [逐行教學 L013][語法] = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`move_req = ...`
// [逐行教學 L013][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`outside = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function。
// [逐行教學 L013][用途] 任一輸入改變就重新計算本區塊；所有路徑都有賦值時不需要記憶狀態；檢查 `key_event` 以決定資料/狀態要走哪一條路徑；硬體上通常形成解碼器/MUX，用狀態、按鍵或索引選擇不同輸出；更新
// [逐行教學 L013][用途] `nr`，下一值由 `pr` 計算；更新 `nc`，下一值由 `pc` 計算；更新 `move_req`為 0/關閉或初始值；更新 `outside`為 0/關閉或初始值。
 always @(*) begin nr=pr;nc=pc;move_req=0;outside=0;if(key_event)case(key_code)
// [逐行教學 L014][語法] `if (pr==0)` 是條件分支：條件為 1 才執行後續敘述；`if (pr==7)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`2` 是
// [逐行教學 L014][語法] case item 標籤；case 值匹配時執行冒號後動作；`move_req = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`outside = ...`
// [逐行教學 L014][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`nr = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`1'b1` =
// [逐行教學 L014][語法] 1-bit 二進位常數；`1'b1` = 1-bit 二進位常數；`==` 是相等比較，結果為 1 或 0。
// [逐行教學 L014][用途] 檢查 `pr==0` 以決定資料/狀態要走哪一條路徑；檢查 `pr==7` 以決定資料/狀態要走哪一條路徑；定義這個選擇值對應的輸出或下一狀態；更新 `move_req`，下一值由 `1` 計算；更新
// [逐行教學 L014][用途] `outside`，下一值由 `1` 計算；更新 `nr`，下一值由 `pr-1'b1` 計算；更新 `nr`，下一值由 `pr+1'b1` 計算。
  2:begin move_req=1;if(pr==0)outside=1;else nr=pr-1'b1;end 8:begin move_req=1;if(pr==7)outside=1;else nr=pr+1'b1;end
// [逐行教學 L015][語法] `if (pc==0)` 是條件分支：條件為 1 才執行後續敘述；`if (pc==7)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`endcase`
// [逐行教學 L015][語法] 結束前面的 case 多路選擇區塊；`default:` 是 case 沒有其他標籤匹配時使用的預設分支；`4` 是 case item 標籤；case 值匹配時執行冒號後動作；`move_req =
// [逐行教學 L015][語法] ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`outside = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`nc
// [逐行教學 L015][語法] = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`1'b1` = 1-bit 二進位常數；`1'b1` = 1-bit 二進位常數；`==`
// [逐行教學 L015][語法] 是相等比較，結果為 1 或 0。
// [逐行教學 L015][用途] 檢查 `pc==0` 以決定資料/狀態要走哪一條路徑；檢查 `pc==7` 以決定資料/狀態要走哪一條路徑；完成目前 case
// [逐行教學 L015][用途] 解碼邏輯；提供未列舉情況的安全輸出/預設動作；定義這個選擇值對應的輸出或下一狀態；更新 `move_req`，下一值由 `1` 計算；更新 `outside`，下一值由 `1` 計算；更新
// [逐行教學 L015][用途] `nc`，下一值由 `pc-1'b1` 計算；更新 `nc`，下一值由 `pc+1'b1` 計算。
  4:begin move_req=1;if(pc==0)outside=1;else nc=pc-1'b1;end 6:begin move_req=1;if(pc==7)outside=1;else nc=pc+1'b1;end default:begin end endcase end
// [逐行教學 L016][語法] `input` 宣告由外部進入模組的輸入埠；`input` 宣告由外部進入模組的輸入埠；`[2:0]` 指定位元範圍；`reg` 宣告可在 `always/function`
// [逐行教學 L016][語法] 中賦值的程序變數；是否形成 DFF 由 always 類型決定；`[7:0]` 指定位元範圍；`function` 宣告可重複呼叫的 Verilog 函式；一般用來封裝組合計算/查表；function
// [逐行教學 L016][語法] 內的 `input` 宣告函式輸入參數；function 內的 `reg` 是函式計算時使用的區域暫存變數。
// [逐行教學 L016][用途] 本段宣告：st；本段宣告：rr；本段宣告：cc；本段宣告：w；建立 `wall_at`，把重複的判斷或查表邏輯集中在一處。
 function wall_at; input st; input [2:0] rr; input [2:0] cc; reg [7:0] w; begin
// [逐行教學 L017][語法] `if (!st)` 是條件分支：條件為 1 才執行後續敘述；`case(rr)` 是多路選擇語法，依選擇值匹配各個 `標籤:`。
// [逐行教學 L017][用途] 檢查 `!st` 以決定資料/狀態要走哪一條路徑；硬體上通常形成解碼器/MUX，用狀態、按鍵或索引選擇不同輸出。
  if(!st)case(rr)
// [逐行教學 L018][語法] `3'd0` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd0` =
// [逐行教學 L018][語法] 3-bit 十進位常數；`8'b11111111` = 8-bit 二進位常數。
// [逐行教學 L018][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11111111` 計算。
   3'd0:w=8'b11111111;
// [逐行教學 L019][語法] `3'd1` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd1` =
// [逐行教學 L019][語法] 3-bit 十進位常數；`8'b10000001` = 8-bit 二進位常數。
// [逐行教學 L019][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10000001` 計算。
   3'd1:w=8'b10000001;
// [逐行教學 L020][語法] `3'd2` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd2` =
// [逐行教學 L020][語法] 3-bit 十進位常數；`8'b11010101` = 8-bit 二進位常數。
// [逐行教學 L020][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11010101` 計算。
   3'd2:w=8'b11010101;
// [逐行教學 L021][語法] `3'd3` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd3` =
// [逐行教學 L021][語法] 3-bit 十進位常數；`8'b10010101` = 8-bit 二進位常數。
// [逐行教學 L021][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10010101` 計算。
   3'd3:w=8'b10010101;
// [逐行教學 L022][語法] `3'd4` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd4` =
// [逐行教學 L022][語法] 3-bit 十進位常數；`8'b10101001` = 8-bit 二進位常數。
// [逐行教學 L022][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10101001` 計算。
   3'd4:w=8'b10101001;
// [逐行教學 L023][語法] `3'd5` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd5` =
// [逐行教學 L023][語法] 3-bit 十進位常數；`8'b10101011` = 8-bit 二進位常數。
// [逐行教學 L023][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10101011` 計算。
   3'd5:w=8'b10101011;
// [逐行教學 L024][語法] `3'd6` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd6` =
// [逐行教學 L024][語法] 3-bit 十進位常數；`8'b10001001` = 8-bit 二進位常數。
// [逐行教學 L024][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10001001` 計算。
   3'd6:w=8'b10001001;
// [逐行教學 L025][語法] `3'd7` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd7` =
// [逐行教學 L025][語法] 3-bit 十進位常數；`8'b11111111` = 8-bit 二進位常數。
// [逐行教學 L025][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11111111` 計算。
   3'd7:w=8'b11111111;
// [逐行教學 L026][語法] `else` 是前面條件都不成立時執行的備援分支；`case(rr)` 是多路選擇語法，依選擇值匹配各個 `標籤:`；`endcase` 結束前面的 case 多路選擇區塊；`default:` 是
// [逐行教學 L026][語法] case 沒有其他標籤匹配時使用的預設分支；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`8'hFF` = 8-bit 十六進位常數。
// [逐行教學 L026][用途] 硬體上通常形成解碼器/MUX，用狀態、按鍵或索引選擇不同輸出；完成目前 case 解碼邏輯；提供未列舉情況的安全輸出/預設動作；更新 `w`，下一值由 `8'hFF` 計算。
   default:w=8'hFF;endcase else case(rr)
// [逐行教學 L027][語法] `3'd0` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd0` =
// [逐行教學 L027][語法] 3-bit 十進位常數；`8'b11111111` = 8-bit 二進位常數。
// [逐行教學 L027][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11111111` 計算。
   3'd0:w=8'b11111111;
// [逐行教學 L028][語法] `3'd1` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd1` =
// [逐行教學 L028][語法] 3-bit 十進位常數；`8'b00110001` = 8-bit 二進位常數。
// [逐行教學 L028][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b00110001` 計算。
   3'd1:w=8'b00110001;
// [逐行教學 L029][語法] `3'd2` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd2` =
// [逐行教學 L029][語法] 3-bit 十進位常數；`8'b10010101` = 8-bit 二進位常數。
// [逐行教學 L029][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10010101` 計算。
   3'd2:w=8'b10010101;
// [逐行教學 L030][語法] `3'd3` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd3` =
// [逐行教學 L030][語法] 3-bit 十進位常數；`8'b11010101` = 8-bit 二進位常數。
// [逐行教學 L030][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11010101` 計算。
   3'd3:w=8'b11010101;
// [逐行教學 L031][語法] `3'd4` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd4` =
// [逐行教學 L031][語法] 3-bit 十進位常數；`8'b10000101` = 8-bit 二進位常數。
// [逐行教學 L031][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10000101` 計算。
   3'd4:w=8'b10000101;
// [逐行教學 L032][語法] `3'd5` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd5` =
// [逐行教學 L032][語法] 3-bit 十進位常數；`8'b11110101` = 8-bit 二進位常數。
// [逐行教學 L032][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11110101` 計算。
   3'd5:w=8'b11110101;
// [逐行教學 L033][語法] `3'd6` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd6` =
// [逐行教學 L033][語法] 3-bit 十進位常數；`8'b10000101` = 8-bit 二進位常數。
// [逐行教學 L033][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10000101` 計算。
   3'd6:w=8'b10000101;
// [逐行教學 L034][語法] `3'd7` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd7` =
// [逐行教學 L034][語法] 3-bit 十進位常數；`8'b11111111` = 8-bit 二進位常數。
// [逐行教學 L034][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11111111` 計算。
   3'd7:w=8'b11111111;
// [逐行教學 L035][語法] `endcase` 結束前面的 case 多路選擇區塊；`default:` 是 case 沒有其他標籤匹配時使用的預設分支；`endfunction` 結束函式定義；`w = ...`
// [逐行教學 L035][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`wall_at = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`8'hFF`
// [逐行教學 L035][語法] = 8-bit 十六進位常數；`[msb:lsb]`/`[index]` 用來選取向量的一段或單一 bit。
// [逐行教學 L035][用途] 完成目前 case 解碼邏輯；提供未列舉情況的安全輸出/預設動作；後續回到一般模組 RTL；更新 `w`，下一值由 `8'hFF` 計算；更新 `wall_at`，下一值由 `w[7-cc]` 計算。
   default:w=8'hFF;endcase wall_at=w[7-cc];end endfunction
// [逐行教學 L036][語法] `always @(posedge/negedge ...)` 是邊緣觸發的時序程序區塊；監看 clk 的上升緣、reset 的上升緣；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L036][用途] 通常綜合成 DFF/暫存器，在指定邊緣更新狀態；Reset 也在敏感度表，因此是非同步重置。
 always @(posedge clk or posedge reset) begin
// [逐行教學 L037][語法] `if (reset)` 是條件分支：條件為 1 才執行後續敘述；`state <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`stage <= ...`
// [逐行教學 L037][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`pr <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`pc <= ...`
// [逐行教學 L037][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新。
// [逐行教學 L037][用途] 檢查 `reset` 以決定資料/狀態要走哪一條路徑；這是重置分支；更新 `state`（FSM 狀態）為 0/關閉或初始值；更新 `stage`為 0/關閉或初始值；更新 `pr`，下一值由 `4`
// [逐行教學 L037][用途] 計算；更新 `pc`，下一值由 `3` 計算。
  if(reset) begin state<=0;stage<=0;pr<=4;pc<=3;end
// [逐行教學 L038][語法] `if (state==0 && move_req)` 是條件分支：條件為 1 才執行後續敘述；`else if` 只在前面 if 不成立時繼續測試下一個條件；`else if(...)`
// [逐行教學 L038][語法] 是子模組實例化；`if` 是這一顆硬體實例名稱；`==` 是相等比較，結果為 1 或 0；`&&` 是邏輯 AND：兩個條件都成立才為真。
// [逐行教學 L038][用途] 檢查 `state==0 && move_req` 以決定資料/狀態要走哪一條路徑；把 `else` 子模組放進目前模組並與內部訊號連線。
  else if(state==0 && move_req) begin
// [逐行教學 L039][語法] `if (outside || wall_at(stage,nr,nc)` 是條件分支：條件為 1 才執行後續敘述；`state <= ...`
// [逐行教學 L039][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`||` 是邏輯 OR：任一條件成立即為真。
// [逐行教學 L039][用途] 檢查 `outside || wall_at(stage,nr,nc` 以決定資料/狀態要走哪一條路徑；更新 `state`（FSM 狀態），下一值由 `1` 計算。
   if(outside || wall_at(stage,nr,nc)) state<=1;
// [逐行教學 L040][語法] `if (!stage && nr==6 && nc==6)` 是條件分支：條件為 1 才執行後續敘述；`else if` 只在前面 if 不成立時繼續測試下一個條件；`else if(...)`
// [逐行教學 L040][語法] 是子模組實例化；`if` 是這一顆硬體實例名稱；`stage <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`pr <= ...`
// [逐行教學 L040][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`pc <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`==` 是相等比較，結果為 1 或
// [逐行教學 L040][語法] 0；`&&` 是邏輯 AND：兩個條件都成立才為真。
// [逐行教學 L040][用途] 檢查 `!stage && nr==6 && nc==6` 以決定資料/狀態要走哪一條路徑；把 `else` 子模組放進目前模組並與內部訊號連線；更新 `stage`，下一值由 `1` 計算；更新
// [逐行教學 L040][用途] `pr`，下一值由 `6` 計算；更新 `pc`，下一值由 `6` 計算。
   else if(!stage && nr==6 && nc==6) begin stage<=1;pr<=6;pc<=6;end
// [逐行教學 L041][語法] `if (stage && nr==1 && nc==0)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`pr <= ...`
// [逐行教學 L041][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`pc <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`state <= ...`
// [逐行教學 L041][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`==` 是相等比較，結果為 1 或 0；`&&` 是邏輯 AND：兩個條件都成立才為真。
// [逐行教學 L041][用途] 檢查 `stage && nr==1 && nc==0` 以決定資料/狀態要走哪一條路徑；更新 `pr`，下一值由 `nr` 計算；更新 `pc`，下一值由 `nc` 計算；更新
// [逐行教學 L041][用途] `state`（FSM 狀態），下一值由 `2` 計算。
   else begin pr<=nr;pc<=nc;if(stage && nr==1 && nc==0)state<=2;end
// [逐行教學 L042][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L042][用途] 關閉目前條件、時序或組合邏輯區塊。
  end
// [逐行教學 L043][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L043][用途] 關閉目前條件、時序或組合邏輯區塊。
 end
// [逐行教學 L044][語法] `always @(*)` 自動把區塊內讀到的訊號加入敏感度表，通常描述組合邏輯；同一行的 `begin` 開始一個多敘述區塊；`if (state==0)` 是條件分支：條件為 1
// [逐行教學 L044][語法] 才執行後續敘述；`if (scan_row==pr)` 是條件分支：條件為 1 才執行後續敘述；`if (state==1)` 是條件分支：條件為 1 才執行後續敘述；`else if` 只在前面
// [逐行教學 L044][語法] if 不成立時繼續測試下一個條件；`red = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`green = ...`
// [逐行教學 L044][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`8'b10000000` = 8-bit 二進位常數；`8'hFF` = 8-bit 十六進位常數；`==`
// [逐行教學 L044][語法] 是相等比較，結果為 1 或 0；`<<`/`>>` 是位移運算，用來移動 bit 圖樣或做 2 的次方倍率變換；`條件 ? A : B` 是三元條件運算子，硬體上等效於 2-to-1 MUX。
// [逐行教學 L044][用途] 任一輸入改變就重新計算本區塊；所有路徑都有賦值時不需要記憶狀態；檢查 `state==0` 以決定資料/狀態要走哪一條路徑；檢查 `scan_row==pr` 以決定資料/狀態要走哪一條路徑；檢查
// [逐行教學 L044][用途] `state==1` 以決定資料/狀態要走哪一條路徑；更新 `red`（紅色欄像素）為 0/關閉或初始值；更新 `green`（綠色欄像素）為 0/關閉或初始值；更新
// [逐行教學 L044][用途] `green`（綠色欄像素），右式用三元運算依條件選一個來源（MUX）；更新 `red`（紅色欄像素），右式使用位移改變圖樣/位置；更新 `green`（綠色欄像素），下一值由 `8'hFF`
// [逐行教學 L044][用途] 計算；更新 `green`（綠色欄像素），下一值由 `smile` 計算。
 always @(*) begin red=0;green=0;if(state==0)begin green=stage?m2row:m1row;if(scan_row==pr)red=(8'b10000000>>pc);end else if(state==1)green=8'hFF;else green=smile;end
// [逐行教學 L045][語法] `endmodule` 與前面的 `module` 配對，結束模組定義。
// [逐行教學 L045][用途] 結束 `red_dot_top` 的 RTL 描述。
endmodule
