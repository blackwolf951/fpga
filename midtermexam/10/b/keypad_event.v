// [逐行教學 HEADER] ===============================================================
// [逐行教學 HEADER] 檔案：keypad_event.v
// [逐行教學 HEADER] 主要模組：keypad_event
// [逐行教學 HEADER] 整體用途：鍵盤事件模組：掃描鍵盤並產生單次 key_event/key_code。
// [逐行教學 HEADER] 每一個「原始實體行」前都加入 [語法] + [用途]，原始程式行本身不修改。
// [逐行教學 HEADER] 若一個原始行塞了多個敘述，註解會把同一行內的宣告/賦值/子模組一起拆解說明。
// [逐行教學 HEADER] ===============================================================
// [逐行教學 L001][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L001][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
// Shared six-state keypad/display scanner with one event per physical press.
// [逐行教學 L002][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L002][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
// A held key does not auto-repeat: key_down is released only after a full 6-state
// [逐行教學 L003][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L003][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
// scan cycle sees no key at all.
// [逐行教學 L004][語法] `module` 宣告一個硬體模組；模組名稱後括號列出對外 I/O。
// [逐行教學 L004][用途] 鍵盤事件模組：掃描鍵盤並產生單次 key_event/key_code。。
module keypad_event(
// [逐行教學 L005][語法] `input` 宣告由外部進入模組的輸入埠。
// [逐行教學 L005][用途] 本段宣告：clk=系統時脈。
    input clk,
// [逐行教學 L006][語法] `input` 宣告由外部進入模組的輸入埠。
// [逐行教學 L006][用途] 本段宣告：reset=重置。
    input reset,
// [逐行教學 L007][語法] `input` 宣告由外部進入模組的輸入埠。
// [逐行教學 L007][用途] 本段宣告：tick_scan。
    input tick_scan,
// [逐行教學 L008][語法] `input` 宣告由外部進入模組的輸入埠；`[2:0]` 指定位元範圍。
// [逐行教學 L008][用途] 本段宣告：column=矩陣鍵盤欄輸入。
    input [2:0] column,
// [逐行教學 L009][語法] `output` 宣告由模組送到外部的輸出埠；`[2:0]` 指定位元範圍；`output reg` 允許此輸出在 always 程序區塊被賦值。
// [逐行教學 L009][用途] 本段宣告：sel=掃描/選擇訊號。
    output reg [2:0] sel,
// [逐行教學 L010][語法] `output` 宣告由模組送到外部的輸出埠；`output reg` 允許此輸出在 always 程序區塊被賦值。
// [逐行教學 L010][用途] 本段宣告：key_event=有效按鍵事件。
    output reg key_event,
// [逐行教學 L011][語法] `output` 宣告由模組送到外部的輸出埠；`[3:0]` 指定位元範圍；`output reg` 允許此輸出在 always 程序區塊被賦值。
// [逐行教學 L011][用途] 本段宣告：key_code=按鍵代碼。
    output reg [3:0] key_code
// [逐行教學 L012][語法] 這是一行 Verilog RTL 敘述；它延續目前區塊的宣告、控制或資料處理。
// [逐行教學 L012][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
);
// [逐行教學 L013][語法] `wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動。
// [逐行教學 L013][用途] 本段宣告：press。
    wire press;
// [逐行教學 L014][語法] `wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[3:0]` 指定位元範圍。
// [逐行教學 L014][用途] 本段宣告：scan_code。
    wire [3:0] scan_code;
// [逐行教學 L015][語法] `reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定。
// [逐行教學 L015][用途] 本段宣告：key_down。
    reg key_down;
// [逐行教學 L016][語法] `reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定。
// [逐行教學 L016][用途] 本段宣告：seen_this_cycle。
    reg seen_this_cycle;
// [逐行教學 L017][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L017][用途] 把不同功能區塊視覺上分開，方便閱讀。

// [逐行教學 L018][語法] `key_decode U_DEC(...)` 是子模組實例化；`U_DEC` 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping。
// [逐行教學 L018][用途] 矩陣鍵盤解碼器。。
    key_decode U_DEC(.sel(sel), .column(column), .press(press), .scan_code(scan_code));
// [逐行教學 L019][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L019][用途] 把不同功能區塊視覺上分開，方便閱讀。

// [逐行教學 L020][語法] `always @(posedge/negedge ...)` 是邊緣觸發的時序程序區塊；監看 clk 的上升緣、reset 的上升緣；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L020][用途] 通常綜合成 DFF/暫存器，在指定邊緣更新狀態；Reset 也在敏感度表，因此是非同步重置。
    always @(posedge clk or posedge reset) begin
// [逐行教學 L021][語法] `if (reset)` 是條件分支：條件為 1 才執行後續敘述。
// [逐行教學 L021][用途] 檢查 `reset` 以決定資料/狀態要走哪一條路徑；這是重置分支。
        if (reset) begin
// [逐行教學 L022][語法] `sel <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`3'b000` = 3-bit 二進位常數。
// [逐行教學 L022][用途] 更新 `sel`（掃描/選擇訊號）為 0/關閉或初始值。
            sel <= 3'b000;
// [逐行教學 L023][語法] `key_event <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b0` = 1-bit 二進位常數。
// [逐行教學 L023][用途] 更新 `key_event`（有效按鍵事件）為 0/關閉或初始值。
            key_event <= 1'b0;
// [逐行教學 L024][語法] `key_code <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`4'hF` = 4-bit 十六進位常數。
// [逐行教學 L024][用途] 更新 `key_code`（按鍵代碼），下一值由 `4'hF` 計算。
            key_code <= 4'hF;
// [逐行教學 L025][語法] `key_down <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b0` = 1-bit 二進位常數。
// [逐行教學 L025][用途] 更新 `key_down`為 0/關閉或初始值。
            key_down <= 1'b0;
// [逐行教學 L026][語法] `seen_this_cycle <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b0` = 1-bit 二進位常數。
// [逐行教學 L026][用途] 更新 `seen_this_cycle`為 0/關閉或初始值。
            seen_this_cycle <= 1'b0;
// [逐行教學 L027][語法] `else` 是前面條件都不成立時執行的備援分支；`end else begin` 先結束前一分支，再開始 else 的多敘述分支。
// [逐行教學 L027][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
        end else begin
// [逐行教學 L028][語法] `key_event <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b0` = 1-bit 二進位常數。
// [逐行教學 L028][用途] 更新 `key_event`（有效按鍵事件）為 0/關閉或初始值。
            key_event <= 1'b0;
// [逐行教學 L029][語法] `if (tick_scan)` 是條件分支：條件為 1 才執行後續敘述。
// [逐行教學 L029][用途] 檢查 `tick_scan` 以決定資料/狀態要走哪一條路徑。
            if (tick_scan) begin
// [逐行教學 L030][語法] `if (press)` 是條件分支：條件為 1 才執行後續敘述。
// [逐行教學 L030][用途] 檢查 `press` 以決定資料/狀態要走哪一條路徑。
                if (press) begin
// [逐行教學 L031][語法] `seen_this_cycle <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` = 1-bit 二進位常數。
// [逐行教學 L031][用途] 更新 `seen_this_cycle`，下一值由 `1'b1` 計算。
                    seen_this_cycle <= 1'b1;
// [逐行教學 L032][語法] `if (!key_down)` 是條件分支：條件為 1 才執行後續敘述。
// [逐行教學 L032][用途] 檢查 `!key_down` 以決定資料/狀態要走哪一條路徑。
                    if (!key_down) begin
// [逐行教學 L033][語法] `key_down <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` = 1-bit 二進位常數。
// [逐行教學 L033][用途] 更新 `key_down`，下一值由 `1'b1` 計算。
                        key_down <= 1'b1;
// [逐行教學 L034][語法] `key_code <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新。
// [逐行教學 L034][用途] 更新 `key_code`（按鍵代碼），下一值由 `scan_code` 計算。
                        key_code <= scan_code;
// [逐行教學 L035][語法] `key_event <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` = 1-bit 二進位常數。
// [逐行教學 L035][用途] 更新 `key_event`（有效按鍵事件），下一值由 `1'b1` 計算。
                        key_event <= 1'b1;
// [逐行教學 L036][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L036][用途] 關閉目前條件、時序或組合邏輯區塊。
                    end
// [逐行教學 L037][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L037][用途] 關閉目前條件、時序或組合邏輯區塊。
                end
// [逐行教學 L038][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L038][用途] 把不同功能區塊視覺上分開，方便閱讀。

// [逐行教學 L039][語法] `if (sel == 3'b101)` 是條件分支：條件為 1 才執行後續敘述；`3'b101` = 3-bit 二進位常數；`==` 是相等比較，結果為 1 或 0。
// [逐行教學 L039][用途] 檢查 `sel == 3'b101` 以決定資料/狀態要走哪一條路徑。
                if (sel == 3'b101) begin
// [逐行教學 L040][語法] `if (!seen_this_cycle && !press)` 是條件分支：條件為 1 才執行後續敘述；`&&` 是邏輯 AND：兩個條件都成立才為真。
// [逐行教學 L040][用途] 檢查 `!seen_this_cycle && !press` 以決定資料/狀態要走哪一條路徑。
                    if (!seen_this_cycle && !press)
// [逐行教學 L041][語法] `key_down <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b0` = 1-bit 二進位常數。
// [逐行教學 L041][用途] 更新 `key_down`為 0/關閉或初始值。
                        key_down <= 1'b0;
// [逐行教學 L042][語法] `seen_this_cycle <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新。
// [逐行教學 L042][用途] 更新 `seen_this_cycle`，下一值由 `press` 計算。
                    seen_this_cycle <= press;
// [逐行教學 L043][語法] `sel <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`3'b000` = 3-bit 二進位常數。
// [逐行教學 L043][用途] 更新 `sel`（掃描/選擇訊號）為 0/關閉或初始值。
                    sel <= 3'b000;
// [逐行教學 L044][語法] `else` 是前面條件都不成立時執行的備援分支；`end else begin` 先結束前一分支，再開始 else 的多敘述分支。
// [逐行教學 L044][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
                end else begin
// [逐行教學 L045][語法] `sel <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` = 1-bit 二進位常數。
// [逐行教學 L045][用途] 更新 `sel`（掃描/選擇訊號）為舊值 +1，完成向上計數。
                    sel <= sel + 1'b1;
// [逐行教學 L046][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L046][用途] 關閉目前條件、時序或組合邏輯區塊。
                end
// [逐行教學 L047][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L047][用途] 關閉目前條件、時序或組合邏輯區塊。
            end
// [逐行教學 L048][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L048][用途] 關閉目前條件、時序或組合邏輯區塊。
        end
// [逐行教學 L049][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L049][用途] 關閉目前條件、時序或組合邏輯區塊。
    end
// [逐行教學 L050][語法] `endmodule` 與前面的 `module` 配對，結束模組定義。
// [逐行教學 L050][用途] 結束 `keypad_event` 的 RTL 描述。
endmodule
