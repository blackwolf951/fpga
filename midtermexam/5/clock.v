// [逐行教學 HEADER] ===============================================================
// [逐行教學 HEADER] 檔案：clock.v
// [逐行教學 HEADER] 主要模組：clock
// [逐行教學 HEADER] 整體用途：計時/倒數核心：依時脈更新數字資料。
// [逐行教學 HEADER] 每一個「原始實體行」前都加入 [語法] + [用途]，原始程式行本身不修改。
// [逐行教學 HEADER] 若一個原始行塞了多個敘述，註解會把同一行內的宣告/賦值/子模組一起拆解說明。
// [逐行教學 HEADER] ===============================================================
// [逐行教學 L001][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L001][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
// Q5 date counter: 21.01.01 -> 48.12.31, handles month length + leap years, then holds.
// [逐行教學 L002][語法] `module` 宣告一個硬體模組；模組名稱後括號列出對外 I/O；埠列表直接出現 `input/output`，屬 ANSI-style 埠宣告。
// [逐行教學 L002][用途] 計時/倒數核心：依時脈更新數字資料。。
module clock(input clk,input reset,input tick_1hz,input enable,
// [逐行教學 L003][語法] `output` 宣告由模組送到外部的輸出埠；`[5:0]` 指定位元範圍；`output reg` 允許此輸出在 always 程序區塊被賦值。
// [逐行教學 L003][用途] 本段宣告：year, month, day。
 output reg [5:0] year,output reg [3:0] month,output reg [5:0] day,output done);
// [逐行教學 L004][語法] `reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定；`[5:0]` 指定位元範圍。
// [逐行教學 L004][用途] 本段宣告：max_day。
 reg [5:0] max_day;
// [逐行教學 L005][語法] `always @(*)` 自動把區塊內讀到的訊號加入敏感度表，通常描述組合邏輯；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L005][用途] 任一輸入改變就重新計算本區塊；所有路徑都有賦值時不需要記憶狀態。
 always @(*) begin
// [逐行教學 L006][語法] `case(month)` 是多路選擇語法，依選擇值匹配各個 `標籤:`。
// [逐行教學 L006][用途] 硬體上通常形成解碼器/MUX，用狀態、按鍵或索引選擇不同輸出。
  case(month)
// [逐行教學 L007][語法] `1` 是 case item 標籤；case 值匹配時執行冒號後動作；`max_day = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function。
// [逐行教學 L007][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `max_day`，下一值由 `31` 計算。
   1,3,5,7,8,10,12:max_day=31;
// [逐行教學 L008][語法] `4` 是 case item 標籤；case 值匹配時執行冒號後動作；`max_day = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function。
// [逐行教學 L008][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `max_day`，下一值由 `30` 計算。
   4,6,9,11:max_day=30;
// [逐行教學 L009][語法] `if (year[1:0]==2'b00)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`2` 是 case item 標籤；case
// [逐行教學 L009][語法] 值匹配時執行冒號後動作；`max_day = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`2'b00` = 2-bit 二進位常數；`==`
// [逐行教學 L009][語法] 是相等比較，結果為 1 或 0；`[msb:lsb]`/`[index]` 用來選取向量的一段或單一 bit。
// [逐行教學 L009][用途] 檢查 `year[1:0]==2'b00` 以決定資料/狀態要走哪一條路徑；定義這個選擇值對應的輸出或下一狀態；更新 `max_day`，下一值由 `29` 計算；更新 `max_day`，下一值由
// [逐行教學 L009][用途] `28` 計算。
   2:begin if(year[1:0]==2'b00) max_day=29; else max_day=28; end
// [逐行教學 L010][語法] `default:` 是 case 沒有其他標籤匹配時使用的預設分支；`max_day = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function。
// [逐行教學 L010][用途] 提供未列舉情況的安全輸出/預設動作；更新 `max_day`，下一值由 `31` 計算。
   default:max_day=31;
// [逐行教學 L011][語法] `endcase` 結束前面的 case 多路選擇區塊。
// [逐行教學 L011][用途] 完成目前 case 解碼邏輯。
  endcase
// [逐行教學 L012][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L012][用途] 關閉目前條件、時序或組合邏輯區塊。
 end
// [逐行教學 L013][語法] `assign 左式 = 右式;` 是 continuous assignment；這裡的 `=` 屬連續指定，不是 always 內的阻塞賦值；`==` 是相等比較，結果為 1 或 0；`&&`
// [逐行教學 L013][語法] 是邏輯 AND：兩個條件都成立才為真。
// [逐行教學 L013][用途] 更新 `done`，下一值由 `(year==48 && month==12 && day==31)` 計算；右式一變化就立即重新驅動左側 wire。
 assign done=(year==48 && month==12 && day==31);
// [逐行教學 L014][語法] `always @(posedge/negedge ...)` 是邊緣觸發的時序程序區塊；監看 clk 的上升緣、reset 的上升緣；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L014][用途] 通常綜合成 DFF/暫存器，在指定邊緣更新狀態；Reset 也在敏感度表，因此是非同步重置。
 always @(posedge clk or posedge reset) begin
// [逐行教學 L015][語法] `if (reset)` 是條件分支：條件為 1 才執行後續敘述；`year <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`month <= ...`
// [逐行教學 L015][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`day <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新。
// [逐行教學 L015][用途] 檢查 `reset` 以決定資料/狀態要走哪一條路徑；這是重置分支；更新 `year`，下一值由 `21` 計算；更新 `month`，下一值由 `1` 計算；更新 `day`，下一值由 `1`
// [逐行教學 L015][用途] 計算。
  if(reset) begin year<=21;month<=1;day<=1;end
// [逐行教學 L016][語法] `if (tick_1hz && enable && !done)` 是條件分支：條件為 1 才執行後續敘述；`else if` 只在前面 if 不成立時繼續測試下一個條件；`else
// [逐行教學 L016][語法] if(...)` 是子模組實例化；`if` 是這一顆硬體實例名稱；`&&` 是邏輯 AND：兩個條件都成立才為真。
// [逐行教學 L016][用途] 檢查 `tick_1hz && enable && !done` 以決定資料/狀態要走哪一條路徑；把 `else` 子模組放進目前模組並與內部訊號連線。
  else if(tick_1hz && enable && !done) begin
// [逐行教學 L017][語法] `if (day<max_day)` 是條件分支：條件為 1 才執行後續敘述；`day <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` = 1-bit
// [逐行教學 L017][語法] 二進位常數。
// [逐行教學 L017][用途] 檢查 `day<max_day` 以決定資料/狀態要走哪一條路徑；更新 `day`為舊值 +1，完成向上計數。
   if(day<max_day) day<=day+1'b1;
// [逐行教學 L018][語法] `else` 是前面條件都不成立時執行的備援分支。
// [逐行教學 L018][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
   else begin
// [逐行教學 L019][語法] `day <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新。
// [逐行教學 L019][用途] 更新 `day`，下一值由 `1` 計算。
    day<=1;
// [逐行教學 L020][語法] `if (month<12)` 是條件分支：條件為 1 才執行後續敘述；`month <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` = 1-bit
// [逐行教學 L020][語法] 二進位常數。
// [逐行教學 L020][用途] 檢查 `month<12` 以決定資料/狀態要走哪一條路徑；更新 `month`為舊值 +1，完成向上計數。
    if(month<12) month<=month+1'b1;
// [逐行教學 L021][語法] `else` 是前面條件都不成立時執行的備援分支；`month <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`year <= ...`
// [逐行教學 L021][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` = 1-bit 二進位常數。
// [逐行教學 L021][用途] 更新 `month`，下一值由 `1` 計算；更新 `year`為舊值 +1，完成向上計數。
    else begin month<=1;year<=year+1'b1;end
// [逐行教學 L022][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L022][用途] 關閉目前條件、時序或組合邏輯區塊。
   end
// [逐行教學 L023][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L023][用途] 關閉目前條件、時序或組合邏輯區塊。
  end
// [逐行教學 L024][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L024][用途] 關閉目前條件、時序或組合邏輯區塊。
 end
// [逐行教學 L025][語法] `endmodule` 與前面的 `module` 配對，結束模組定義。
// [逐行教學 L025][用途] 結束 `clock` 的 RTL 描述。
endmodule
