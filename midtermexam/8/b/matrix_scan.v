// [逐行教學 HEADER] ===============================================================
// [逐行教學 HEADER] 檔案：matrix_scan.v
// [逐行教學 HEADER] 主要模組：matrix_scan
// [逐行教學 HEADER] 整體用途：8×8 LED Matrix 掃描器。
// [逐行教學 HEADER] 每一個「原始實體行」前都加入 [語法] + [用途]，原始程式行本身不修改。
// [逐行教學 HEADER] 若一個原始行塞了多個敘述，註解會把同一行內的宣告/賦值/子模組一起拆解說明。
// [逐行教學 HEADER] ===============================================================
// [逐行教學 L001][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L001][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
// Active-high 8x8 row scanner used by the supplied LP-2900 LAB4/LAB8 projects.
// [逐行教學 L002][語法] `module` 宣告一個硬體模組；模組名稱後括號列出對外 I/O。
// [逐行教學 L002][用途] 8×8 LED Matrix 掃描器。。
module matrix_scan(
// [逐行教學 L003][語法] `input` 宣告由外部進入模組的輸入埠。
// [逐行教學 L003][用途] 本段宣告：clk=系統時脈。
    input clk,
// [逐行教學 L004][語法] `input` 宣告由外部進入模組的輸入埠。
// [逐行教學 L004][用途] 本段宣告：reset=重置。
    input reset,
// [逐行教學 L005][語法] `input` 宣告由外部進入模組的輸入埠。
// [逐行教學 L005][用途] 本段宣告：tick_scan。
    input tick_scan,
// [逐行教學 L006][語法] `output` 宣告由模組送到外部的輸出埠；`[2:0]` 指定位元範圍；`output reg` 允許此輸出在 always 程序區塊被賦值。
// [逐行教學 L006][用途] 本段宣告：row_idx=目前掃描列索引。
    output reg [2:0] row_idx,
// [逐行教學 L007][語法] `output` 宣告由模組送到外部的輸出埠；`[7:0]` 指定位元範圍；`output reg` 允許此輸出在 always 程序區塊被賦值。
// [逐行教學 L007][用途] 本段宣告：row=LED Matrix 列選。
    output reg [7:0] row
// [逐行教學 L008][語法] 這是一行 Verilog RTL 敘述；它延續目前區塊的宣告、控制或資料處理。
// [逐行教學 L008][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
);
// [逐行教學 L009][語法] `always @(posedge/negedge ...)` 是邊緣觸發的時序程序區塊；監看 clk 的上升緣、reset 的上升緣；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L009][用途] 通常綜合成 DFF/暫存器，在指定邊緣更新狀態；Reset 也在敏感度表，因此是非同步重置。
    always @(posedge clk or posedge reset) begin
// [逐行教學 L010][語法] `if (reset)` 是條件分支：條件為 1 才執行後續敘述。
// [逐行教學 L010][用途] 檢查 `reset` 以決定資料/狀態要走哪一條路徑；這是重置分支。
        if (reset) begin
// [逐行教學 L011][語法] `row_idx <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`3'd0` = 3-bit 十進位常數。
// [逐行教學 L011][用途] 更新 `row_idx`（目前掃描列索引），下一值由 `3'd0` 計算。
            row_idx <= 3'd0;
// [逐行教學 L012][語法] `row <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`8'b1000_0000` = 8-bit 二進位常數。
// [逐行教學 L012][用途] 更新 `row`（LED Matrix 列選），下一值由 `8'b1000_0000` 計算。
            row <= 8'b1000_0000;
// [逐行教學 L013][語法] `if (tick_scan)` 是條件分支：條件為 1 才執行後續敘述；`else if` 只在前面 if 不成立時繼續測試下一個條件。
// [逐行教學 L013][用途] 檢查 `tick_scan` 以決定資料/狀態要走哪一條路徑。
        end else if (tick_scan) begin
// [逐行教學 L014][語法] `if (row_idx == 3'd7)` 是條件分支：條件為 1 才執行後續敘述；`3'd7` = 3-bit 十進位常數；`==` 是相等比較，結果為 1 或 0。
// [逐行教學 L014][用途] 檢查 `row_idx == 3'd7` 以決定資料/狀態要走哪一條路徑。
            if (row_idx == 3'd7) begin
// [逐行教學 L015][語法] `row_idx <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`3'd0` = 3-bit 十進位常數。
// [逐行教學 L015][用途] 更新 `row_idx`（目前掃描列索引），下一值由 `3'd0` 計算。
                row_idx <= 3'd0;
// [逐行教學 L016][語法] `row <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`8'b1000_0000` = 8-bit 二進位常數。
// [逐行教學 L016][用途] 更新 `row`（LED Matrix 列選），下一值由 `8'b1000_0000` 計算。
                row <= 8'b1000_0000;
// [逐行教學 L017][語法] `else` 是前面條件都不成立時執行的備援分支；`end else begin` 先結束前一分支，再開始 else 的多敘述分支。
// [逐行教學 L017][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
            end else begin
// [逐行教學 L018][語法] `row_idx <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` = 1-bit 二進位常數。
// [逐行教學 L018][用途] 更新 `row_idx`（目前掃描列索引）為舊值 +1，完成向上計數。
                row_idx <= row_idx + 1'b1;
// [逐行教學 L019][語法] `row <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b0` = 1-bit 二進位常數；`{...}` 是 concatenation 位元串接，可把多段
// [逐行教學 L019][語法] bit 合併/重排；`[msb:lsb]`/`[index]` 用來選取向量的一段或單一 bit。
// [逐行教學 L019][用途] 更新 `row`（LED Matrix 列選），右式用 `{...}` 串接/重排多段位元。
                row <= {1'b0, row[7:1]};
// [逐行教學 L020][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L020][用途] 關閉目前條件、時序或組合邏輯區塊。
            end
// [逐行教學 L021][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L021][用途] 關閉目前條件、時序或組合邏輯區塊。
        end
// [逐行教學 L022][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L022][用途] 關閉目前條件、時序或組合邏輯區塊。
    end
// [逐行教學 L023][語法] `endmodule` 與前面的 `module` 配對，結束模組定義。
// [逐行教學 L023][用途] 結束 `matrix_scan` 的 RTL 描述。
endmodule
