// [逐行教學 HEADER] ===============================================================
// [逐行教學 HEADER] 檔案：red_dot_top.v
// [逐行教學 HEADER] 主要模組：red_dot_top
// [逐行教學 HEADER] 整體用途：紅點迷宮頂層：整合鍵盤、移動、撞牆、倒數與 LED Matrix。
// [逐行教學 HEADER] 每一個「原始實體行」前都加入 [語法] + [用途]，原始程式行本身不修改。
// [逐行教學 HEADER] 若一個原始行塞了多個敘述，註解會把同一行內的宣告/賦值/子模組一起拆解說明。
// [逐行教學 HEADER] ===============================================================
// [逐行教學 L001][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L001][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
// TOP Q8: exact exam maze, 15-second countdown. 2/4/6/8 = up/left/right/down.
// [逐行教學 L002][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L002][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
// state 0 PLAY, 1 FAIL (all green), 2 WIN (green smile).
// [逐行教學 L003][語法] `module` 宣告一個硬體模組；模組名稱後括號列出對外 I/O；埠列表直接出現 `input/output`，屬 ANSI-style 埠宣告；`[msb:lsb]`/`[index]`
// [逐行教學 L003][語法] 用來選取向量的一段或單一 bit。
// [逐行教學 L003][用途] 紅點迷宮頂層：整合鍵盤、移動、撞牆、倒數與 LED Matrix。。
module red_dot_top(input clk,input reset,input [2:0] column,
// [逐行教學 L004][語法] `output` 宣告由模組送到外部的輸出埠；`[7:0]` 指定位元範圍；`output reg` 允許此輸出在 always 程序區塊被賦值。
// [逐行教學 L004][用途] 本段宣告：red=紅色欄像素；green=綠色欄像素；row=LED Matrix 列選；sel=掃描/選擇訊號。
 output reg [7:0] red,output reg [7:0] green,output [7:0] row,output [2:0] sel,output [6:0] seg7);
// [逐行教學 L005][語法] `wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[3:0]` 指定位元範圍；`wire`
// [逐行教學 L005][語法] 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[2:0]` 指定位元範圍。
// [逐行教學 L005][用途] 本段宣告：clk_1hz, clk_keyscan, clk_matrix；本段宣告：tick_1hz,
// [逐行教學 L005][用途] tick_keyscan；本段宣告：key_event=有效按鍵事件；本段宣告：key_code=按鍵代碼；本段宣告：scan_row=目前掃描列索引。
 wire clk_1hz,clk_keyscan,clk_matrix; wire tick_1hz,tick_keyscan; wire key_event; wire [3:0] key_code; wire [2:0] scan_row;
// [逐行教學 L006][語法] `wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[7:0]` 指定位元範圍；`reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由
// [逐行教學 L006][語法] always 類型決定；`[1:0]` 指定位元範圍；`reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定；`[2:0]`
// [逐行教學 L006][語法] 指定位元範圍；`reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定；`[5:0]` 指定位元範圍。
// [逐行教學 L006][用途] 本段宣告：wall_row, smile_row；本段宣告：state=FSM 狀態；本段宣告：pr, pc；本段宣告：timer=倒數值。
 wire [7:0] wall_row,smile_row; reg [1:0] state; reg [2:0] pr,pc; reg [5:0] timer;
// [逐行教學 L007][語法] `reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定；`[2:0]` 指定位元範圍；`reg` 宣告可在
// [逐行教學 L007][語法] `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定；`reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由
// [逐行教學 L007][語法] always 類型決定；`[3:0]` 指定位元範圍。
// [逐行教學 L007][用途] 本段宣告：nr, nc；本段宣告：move_req, outside；本段宣告：digit=目前七段顯示數字。
 reg [2:0] nr,nc; reg move_req,outside; reg [3:0] digit;
// [逐行教學 L008][語法] `freq_div_1s U1(...)` 是子模組實例化；`U1` 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping。
// [逐行教學 L008][用途] 1 秒時基產生器：把系統時脈轉成約 1 Hz 的秒節拍。。
 freq_div_1s U1(.clk_in(clk),.reset(reset),.clk_out(clk_1hz));
// [逐行教學 L009][語法] `freq_div UKT(...)` 是子模組實例化；`UKT` 是這一顆硬體實例名稱；`#(14)` 是 parameter
// [逐行教學 L009][語法] override，用新值取代子模組預設參數；`.port(signal)` 是具名 port mapping。
// [逐行教學 L009][用途] 時脈分頻器：利用二進位計數器的某一位，把 FPGA 高速系統時脈降成較慢時脈。。
 freq_div #(14) UKT(.clk_in(clk),.reset(reset),.clk_out(clk_keyscan));
// [逐行教學 L010][語法] `freq_div UMT(...)` 是子模組實例化；`UMT` 是這一顆硬體實例名稱；`#(10)` 是 parameter
// [逐行教學 L010][語法] override，用新值取代子模組預設參數；`.port(signal)` 是具名 port mapping。
// [逐行教學 L010][用途] 時脈分頻器：利用二進位計數器的某一位，把 FPGA 高速系統時脈降成較慢時脈。。
 freq_div #(10) UMT(.clk_in(clk),.reset(reset),.clk_out(clk_matrix));
// [逐行教學 L011][語法] `reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由 always 類型決定。
// [逐行教學 L011][用途] 本段宣告：clk_1hz_d, clk_keyscan_d。
 reg clk_1hz_d, clk_keyscan_d;
// [逐行教學 L012][語法] `assign 左式 = 右式;` 是 continuous assignment；這裡的 `=` 屬連續指定，不是 always 內的阻塞賦值；單一 `&` 是逐位元 AND；`~` 是逐位元反相。
// [逐行教學 L012][用途] 更新 `tick_1hz`，下一值由 `clk_1hz & ~clk_1hz_d` 計算；右式一變化就立即重新驅動左側 wire。
 assign tick_1hz = clk_1hz & ~clk_1hz_d;
// [逐行教學 L013][語法] `assign 左式 = 右式;` 是 continuous assignment；這裡的 `=` 屬連續指定，不是 always 內的阻塞賦值；單一 `&` 是逐位元 AND；`~` 是逐位元反相。
// [逐行教學 L013][用途] 更新 `tick_keyscan`，下一值由 `clk_keyscan & ~clk_keyscan_d` 計算；右式一變化就立即重新驅動左側 wire。
 assign tick_keyscan = clk_keyscan & ~clk_keyscan_d;
// [逐行教學 L014][語法] `always @(posedge/negedge ...)` 是邊緣觸發的時序程序區塊；監看 clk 的上升緣、reset 的上升緣；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L014][用途] 通常綜合成 DFF/暫存器，在指定邊緣更新狀態；Reset 也在敏感度表，因此是非同步重置。
 always @(posedge clk or posedge reset) begin
// [逐行教學 L015][語法] `if (reset)` 是條件分支：條件為 1 才執行後續敘述；`clk_1hz_d <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`clk_keyscan_d
// [逐行教學 L015][語法] <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b0` = 1-bit 二進位常數；`1'b0` = 1-bit 二進位常數。
// [逐行教學 L015][用途] 檢查 `reset` 以決定資料/狀態要走哪一條路徑；這是重置分支；更新 `clk_1hz_d`為 0/關閉或初始值；更新 `clk_keyscan_d`為 0/關閉或初始值。
  if(reset) begin clk_1hz_d<=1'b0; clk_keyscan_d<=1'b0; end
// [逐行教學 L016][語法] `else` 是前面條件都不成立時執行的備援分支；`clk_1hz_d <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`clk_keyscan_d <= ...`
// [逐行教學 L016][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新。
// [逐行教學 L016][用途] 更新 `clk_1hz_d`，下一值由 `clk_1hz` 計算；更新 `clk_keyscan_d`，下一值由 `clk_keyscan` 計算。
  else begin clk_1hz_d<=clk_1hz; clk_keyscan_d<=clk_keyscan; end
// [逐行教學 L017][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L017][用途] 關閉目前條件、時序或組合邏輯區塊。
 end
// [逐行教學 L018][語法] `keypad_event UK(...)` 是子模組實例化；`UK` 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping。
// [逐行教學 L018][用途] 鍵盤事件模組：掃描鍵盤並產生單次 key_event/key_code。。
 keypad_event UK(.clk(clk),.reset(reset),.tick_scan(tick_keyscan),.column(column),.sel(sel),.key_event(key_event),.key_code(key_code));
// [逐行教學 L019][語法] `matrix_scan US(...)` 是子模組實例化；`US` 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping；`1'b1` = 1-bit 二進位常數。
// [逐行教學 L019][用途] 8×8 LED Matrix 掃描器。。
 matrix_scan US(.clk(clk_matrix),.reset(reset),.tick_scan(1'b1),.row_idx(scan_row),.row(row));
// [逐行教學 L020][語法] `map UM(...)` 是子模組實例化；`UM` 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping；`smile_rom USM(...)`
// [逐行教學 L020][語法] 是子模組實例化；`USM` 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping。
// [逐行教學 L020][用途] 迷宮地圖 ROM。；過關笑臉 ROM。。
 map UM(.row_idx(scan_row),.data(wall_row)); smile_rom USM(.row_idx(scan_row),.data(smile_row));
// [逐行教學 L021][語法] `always @(*)` 自動把區塊內讀到的訊號加入敏感度表，通常描述組合邏輯；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L021][用途] 任一輸入改變就重新計算本區塊；所有路徑都有賦值時不需要記憶狀態。
 always @(*) begin
// [逐行教學 L022][語法] `nr = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`nc = ...`
// [逐行教學 L022][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`move_req = ...`
// [逐行教學 L022][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`outside = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`1'b0` =
// [逐行教學 L022][語法] 1-bit 二進位常數；`1'b0` = 1-bit 二進位常數。
// [逐行教學 L022][用途] 更新 `nr`，下一值由 `pr` 計算；更新 `nc`，下一值由 `pc` 計算；更新 `move_req`為 0/關閉或初始值；更新 `outside`為 0/關閉或初始值。
  nr=pr;nc=pc;move_req=1'b0;outside=1'b0;
// [逐行教學 L023][語法] `if (key_event)` 是條件分支：條件為 1 才執行後續敘述。
// [逐行教學 L023][用途] 檢查 `key_event` 以決定資料/狀態要走哪一條路徑。
  if(key_event) begin
// [逐行教學 L024][語法] `case(key_code)` 是多路選擇語法，依選擇值匹配各個 `標籤:`。
// [逐行教學 L024][用途] 硬體上通常形成解碼器/MUX，用狀態、按鍵或索引選擇不同輸出。
   case(key_code)
// [逐行教學 L025][語法] `if (pr==0)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`4'd2` 是 case item 標籤；case
// [逐行教學 L025][語法] 值匹配時執行冒號後動作；`move_req = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`outside = ...`
// [逐行教學 L025][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`nr = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`4'd2` =
// [逐行教學 L025][語法] 4-bit 十進位常數；`1'b1` = 1-bit 二進位常數；`1'b1` = 1-bit 二進位常數；`1'b1` = 1-bit 二進位常數；`==` 是相等比較，結果為 1 或 0。
// [逐行教學 L025][用途] 檢查 `pr==0` 以決定資料/狀態要走哪一條路徑；定義這個選擇值對應的輸出或下一狀態；更新 `move_req`，下一值由 `1'b1` 計算；更新 `outside`，下一值由 `1'b1`
// [逐行教學 L025][用途] 計算；更新 `nr`，下一值由 `pr-1'b1` 計算。
    4'd2:begin move_req=1'b1;if(pr==0)outside=1'b1;else nr=pr-1'b1;end
// [逐行教學 L026][語法] `if (pr==7)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`4'd8` 是 case item 標籤；case
// [逐行教學 L026][語法] 值匹配時執行冒號後動作；`move_req = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`outside = ...`
// [逐行教學 L026][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`nr = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`4'd8` =
// [逐行教學 L026][語法] 4-bit 十進位常數；`1'b1` = 1-bit 二進位常數；`1'b1` = 1-bit 二進位常數；`1'b1` = 1-bit 二進位常數；`==` 是相等比較，結果為 1 或 0。
// [逐行教學 L026][用途] 檢查 `pr==7` 以決定資料/狀態要走哪一條路徑；定義這個選擇值對應的輸出或下一狀態；更新 `move_req`，下一值由 `1'b1` 計算；更新 `outside`，下一值由 `1'b1`
// [逐行教學 L026][用途] 計算；更新 `nr`，下一值由 `pr+1'b1` 計算。
    4'd8:begin move_req=1'b1;if(pr==7)outside=1'b1;else nr=pr+1'b1;end
// [逐行教學 L027][語法] `if (pc==0)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`4'd4` 是 case item 標籤；case
// [逐行教學 L027][語法] 值匹配時執行冒號後動作；`move_req = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`outside = ...`
// [逐行教學 L027][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`nc = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`4'd4` =
// [逐行教學 L027][語法] 4-bit 十進位常數；`1'b1` = 1-bit 二進位常數；`1'b1` = 1-bit 二進位常數；`1'b1` = 1-bit 二進位常數；`==` 是相等比較，結果為 1 或 0。
// [逐行教學 L027][用途] 檢查 `pc==0` 以決定資料/狀態要走哪一條路徑；定義這個選擇值對應的輸出或下一狀態；更新 `move_req`，下一值由 `1'b1` 計算；更新 `outside`，下一值由 `1'b1`
// [逐行教學 L027][用途] 計算；更新 `nc`，下一值由 `pc-1'b1` 計算。
    4'd4:begin move_req=1'b1;if(pc==0)outside=1'b1;else nc=pc-1'b1;end
// [逐行教學 L028][語法] `if (pc==7)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`4'd6` 是 case item 標籤；case
// [逐行教學 L028][語法] 值匹配時執行冒號後動作；`move_req = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`outside = ...`
// [逐行教學 L028][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`nc = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`4'd6` =
// [逐行教學 L028][語法] 4-bit 十進位常數；`1'b1` = 1-bit 二進位常數；`1'b1` = 1-bit 二進位常數；`1'b1` = 1-bit 二進位常數；`==` 是相等比較，結果為 1 或 0。
// [逐行教學 L028][用途] 檢查 `pc==7` 以決定資料/狀態要走哪一條路徑；定義這個選擇值對應的輸出或下一狀態；更新 `move_req`，下一值由 `1'b1` 計算；更新 `outside`，下一值由 `1'b1`
// [逐行教學 L028][用途] 計算；更新 `nc`，下一值由 `pc+1'b1` 計算。
    4'd6:begin move_req=1'b1;if(pc==7)outside=1'b1;else nc=pc+1'b1;end
// [逐行教學 L029][語法] `default:` 是 case 沒有其他標籤匹配時使用的預設分支。
// [逐行教學 L029][用途] 提供未列舉情況的安全輸出/預設動作。
    default:begin end
// [逐行教學 L030][語法] `endcase` 結束前面的 case 多路選擇區塊。
// [逐行教學 L030][用途] 完成目前 case 解碼邏輯。
   endcase
// [逐行教學 L031][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L031][用途] 關閉目前條件、時序或組合邏輯區塊。
  end
// [逐行教學 L032][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L032][用途] 關閉目前條件、時序或組合邏輯區塊。
 end
// [逐行教學 L033][語法] `input` 宣告由外部進入模組的輸入埠；`[2:0]` 指定位元範圍；`reg` 宣告可在 `always/function` 中賦值的程序變數；是否形成 DFF 由 always
// [逐行教學 L033][語法] 類型決定；`[7:0]` 指定位元範圍；`function` 宣告可重複呼叫的 Verilog 函式；一般用來封裝組合計算/查表；function 內的 `input`
// [逐行教學 L033][語法] 宣告函式輸入參數；function 內的 `reg` 是函式計算時使用的區域暫存變數。
// [逐行教學 L033][用途] 本段宣告：rr；本段宣告：cc；本段宣告：w；建立 `wall_at`，把重複的判斷或查表邏輯集中在一處。
 function wall_at; input [2:0] rr; input [2:0] cc; reg [7:0] w; begin
// [逐行教學 L034][語法] `case(rr)` 是多路選擇語法，依選擇值匹配各個 `標籤:`。
// [逐行教學 L034][用途] 硬體上通常形成解碼器/MUX，用狀態、按鍵或索引選擇不同輸出。
  case(rr)
// [逐行教學 L035][語法] `3'd0` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd0` =
// [逐行教學 L035][語法] 3-bit 十進位常數；`8'b11111111` = 8-bit 二進位常數。
// [逐行教學 L035][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11111111` 計算。
   3'd0:w=8'b11111111;
// [逐行教學 L036][語法] `3'd1` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd1` =
// [逐行教學 L036][語法] 3-bit 十進位常數；`8'b00010001` = 8-bit 二進位常數。
// [逐行教學 L036][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b00010001` 計算。
   3'd1:w=8'b00010001;
// [逐行教學 L037][語法] `3'd2` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd2` =
// [逐行教學 L037][語法] 3-bit 十進位常數；`8'b11000101` = 8-bit 二進位常數。
// [逐行教學 L037][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11000101` 計算。
   3'd2:w=8'b11000101;
// [逐行教學 L038][語法] `3'd3` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd3` =
// [逐行教學 L038][語法] 3-bit 十進位常數；`8'b11111101` = 8-bit 二進位常數。
// [逐行教學 L038][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11111101` 計算。
   3'd3:w=8'b11111101;
// [逐行教學 L039][語法] `3'd4` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd4` =
// [逐行教學 L039][語法] 3-bit 十進位常數；`8'b10000001` = 8-bit 二進位常數。
// [逐行教學 L039][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10000001` 計算。
   3'd4:w=8'b10000001;
// [逐行教學 L040][語法] `3'd5` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd5` =
// [逐行教學 L040][語法] 3-bit 十進位常數；`8'b10111111` = 8-bit 二進位常數。
// [逐行教學 L040][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10111111` 計算。
   3'd5:w=8'b10111111;
// [逐行教學 L041][語法] `3'd6` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd6` =
// [逐行教學 L041][語法] 3-bit 十進位常數；`8'b10000000` = 8-bit 二進位常數。
// [逐行教學 L041][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b10000000` 計算。
   3'd6:w=8'b10000000;
// [逐行教學 L042][語法] `3'd7` 是 case item 標籤；case 值匹配時執行冒號後動作；`w = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'd7` =
// [逐行教學 L042][語法] 3-bit 十進位常數；`8'b11111111` = 8-bit 二進位常數。
// [逐行教學 L042][用途] 定義這個選擇值對應的輸出或下一狀態；更新 `w`，下一值由 `8'b11111111` 計算。
   3'd7:w=8'b11111111;
// [逐行教學 L043][語法] `endcase` 結束前面的 case 多路選擇區塊；`default:` 是 case 沒有其他標籤匹配時使用的預設分支；`endfunction` 結束函式定義；`w = ...`
// [逐行教學 L043][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`wall_at = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`8'hFF`
// [逐行教學 L043][語法] = 8-bit 十六進位常數；`[msb:lsb]`/`[index]` 用來選取向量的一段或單一 bit。
// [逐行教學 L043][用途] 完成目前 case 解碼邏輯；提供未列舉情況的安全輸出/預設動作；後續回到一般模組 RTL；更新 `w`，下一值由 `8'hFF` 計算；更新 `wall_at`，下一值由 `w[7-cc]` 計算。
   default:w=8'hFF;endcase wall_at=w[7-cc];end endfunction
// [逐行教學 L044][語法] `always @(posedge/negedge ...)` 是邊緣觸發的時序程序區塊；監看 clk 的上升緣、reset 的上升緣；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L044][用途] 通常綜合成 DFF/暫存器，在指定邊緣更新狀態；Reset 也在敏感度表，因此是非同步重置。
 always @(posedge clk or posedge reset) begin
// [逐行教學 L045][語法] `if (reset)` 是條件分支：條件為 1 才執行後續敘述；`state <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`pr <= ...`
// [逐行教學 L045][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`pc <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`timer <= ...`
// [逐行教學 L045][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新。
// [逐行教學 L045][用途] 檢查 `reset` 以決定資料/狀態要走哪一條路徑；這是重置分支；更新 `state`（FSM 狀態）為 0/關閉或初始值；更新 `pr`，下一值由 `6` 計算；更新 `pc`，下一值由 `7`
// [逐行教學 L045][用途] 計算；更新 `timer`（倒數值），下一值由 `15` 計算。
  if(reset) begin state<=0;pr<=6;pc<=7;timer<=15;end
// [逐行教學 L046][語法] `if (state==0)` 是條件分支：條件為 1 才執行後續敘述；`else if` 只在前面 if 不成立時繼續測試下一個條件；`else if(...)` 是子模組實例化；`if`
// [逐行教學 L046][語法] 是這一顆硬體實例名稱；`==` 是相等比較，結果為 1 或 0。
// [逐行教學 L046][用途] 檢查 `state==0` 以決定資料/狀態要走哪一條路徑；把 `else` 子模組放進目前模組並與內部訊號連線。
  else if(state==0) begin
// [逐行教學 L047][語法] `if (move_req)` 是條件分支：條件為 1 才執行後續敘述。
// [逐行教學 L047][用途] 檢查 `move_req` 以決定資料/狀態要走哪一條路徑。
   if(move_req) begin
// [逐行教學 L048][語法] `if (outside || wall_at(nr,nc)` 是條件分支：條件為 1 才執行後續敘述；`state <= ...`
// [逐行教學 L048][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`||` 是邏輯 OR：任一條件成立即為真。
// [逐行教學 L048][用途] 檢查 `outside || wall_at(nr,nc` 以決定資料/狀態要走哪一條路徑；更新 `state`（FSM 狀態），下一值由 `1` 計算。
    if(outside || wall_at(nr,nc)) state<=1;
// [逐行教學 L049][語法] `if (nr==1 && nc==0)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`pr <= ...`
// [逐行教學 L049][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`pc <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`state <= ...`
// [逐行教學 L049][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`==` 是相等比較，結果為 1 或 0；`&&` 是邏輯 AND：兩個條件都成立才為真。
// [逐行教學 L049][用途] 檢查 `nr==1 && nc==0` 以決定資料/狀態要走哪一條路徑；更新 `pr`，下一值由 `nr` 計算；更新 `pc`，下一值由 `nc` 計算；更新 `state`（FSM
// [逐行教學 L049][用途] 狀態），下一值由 `2` 計算。
    else begin pr<=nr;pc<=nc;if(nr==1 && nc==0) state<=2;end
// [逐行教學 L050][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L050][用途] 關閉目前條件、時序或組合邏輯區塊。
   end
// [逐行教學 L051][語法] `if (tick_1hz)` 是條件分支：條件為 1 才執行後續敘述；`else if` 只在前面 if 不成立時繼續測試下一個條件；`else if(...)` 是子模組實例化；`if`
// [逐行教學 L051][語法] 是這一顆硬體實例名稱。
// [逐行教學 L051][用途] 檢查 `tick_1hz` 以決定資料/狀態要走哪一條路徑；把 `else` 子模組放進目前模組並與內部訊號連線。
   else if(tick_1hz) begin
// [逐行教學 L052][語法] `if (timer>1)` 是條件分支：條件為 1 才執行後續敘述；`else` 是前面條件都不成立時執行的備援分支；`timer <= ...`
// [逐行教學 L052][語法] 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`state <= ...` 使用非阻塞賦值；常用於時序邏輯，先用舊值計算、事件結束時一起更新；`1'b1` = 1-bit
// [逐行教學 L052][語法] 二進位常數。
// [逐行教學 L052][用途] 檢查 `timer>1` 以決定資料/狀態要走哪一條路徑；更新 `timer`（倒數值）為舊值 -1，完成向下計數；更新 `timer`（倒數值）為 0/關閉或初始值；更新 `state`（FSM
// [逐行教學 L052][用途] 狀態），下一值由 `1` 計算。
    if(timer>1) timer<=timer-1'b1; else begin timer<=0;state<=1;end
// [逐行教學 L053][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L053][用途] 關閉目前條件、時序或組合邏輯區塊。
   end
// [逐行教學 L054][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L054][用途] 關閉目前條件、時序或組合邏輯區塊。
  end
// [逐行教學 L055][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L055][用途] 關閉目前條件、時序或組合邏輯區塊。
 end
// [逐行教學 L056][語法] `always @(*)` 自動把區塊內讀到的訊號加入敏感度表，通常描述組合邏輯；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L056][用途] 任一輸入改變就重新計算本區塊；所有路徑都有賦值時不需要記憶狀態。
 always @(*) begin
// [逐行教學 L057][語法] `red = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`green = ...`
// [逐行教學 L057][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`8'h00` = 8-bit 十六進位常數；`8'h00` = 8-bit 十六進位常數。
// [逐行教學 L057][用途] 更新 `red`（紅色欄像素）為 0/關閉或初始值；更新 `green`（綠色欄像素）為 0/關閉或初始值。
  red=8'h00; green=8'h00;
// [逐行教學 L058][語法] `if (state==0)` 是條件分支：條件為 1 才執行後續敘述；`if (scan_row==pr)` 是條件分支：條件為 1 才執行後續敘述；`green = ...`
// [逐行教學 L058][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`red = ...`
// [逐行教學 L058][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`8'b1000_0000` = 8-bit 二進位常數；`==` 是相等比較，結果為 1 或 0；`<<`/`>>`
// [逐行教學 L058][語法] 是位移運算，用來移動 bit 圖樣或做 2 的次方倍率變換。
// [逐行教學 L058][用途] 檢查 `state==0` 以決定資料/狀態要走哪一條路徑；檢查 `scan_row==pr` 以決定資料/狀態要走哪一條路徑；更新 `green`（綠色欄像素），下一值由 `wall_row`
// [逐行教學 L058][用途] 計算；更新 `red`（紅色欄像素），右式使用位移改變圖樣/位置。
  if(state==0) begin green=wall_row;if(scan_row==pr)red=(8'b1000_0000>>pc);end
// [逐行教學 L059][語法] `if (state==1)` 是條件分支：條件為 1 才執行後續敘述；`else if` 只在前面 if 不成立時繼續測試下一個條件；`else if(...)` 是子模組實例化；`if`
// [逐行教學 L059][語法] 是這一顆硬體實例名稱；`green = ...` 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`8'hFF` = 8-bit 十六進位常數；`==` 是相等比較，結果為
// [逐行教學 L059][語法] 1 或 0。
// [逐行教學 L059][用途] 檢查 `state==1` 以決定資料/狀態要走哪一條路徑；把 `else` 子模組放進目前模組並與內部訊號連線；更新 `green`（綠色欄像素），下一值由 `8'hFF` 計算；更新
// [逐行教學 L059][用途] `green`（綠色欄像素），下一值由 `smile_row` 計算。
  else if(state==1) green=8'hFF; else green=smile_row;
// [逐行教學 L060][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L060][用途] 關閉目前條件、時序或組合邏輯區塊。
 end
// [逐行教學 L061][語法] `always @(*)` 自動把區塊內讀到的訊號加入敏感度表，通常描述組合邏輯；同一行的 `begin` 開始一個多敘述區塊。
// [逐行教學 L061][用途] 任一輸入改變就重新計算本區塊；所有路徑都有賦值時不需要記憶狀態。
 always @(*) begin
// [逐行教學 L062][語法] `if (sel==3'b100)` 是條件分支：條件為 1 才執行後續敘述；`if (sel==3'b101)` 是條件分支：條件為 1 才執行後續敘述；`else if` 只在前面 if
// [逐行教學 L062][語法] 不成立時繼續測試下一個條件；`else if(...)` 是子模組實例化；`if` 是這一顆硬體實例名稱；`digit = ...`
// [逐行教學 L062][語法] 使用阻塞賦值；在程序區塊內依順序立即更新，常用於組合計算/function；`3'b100` = 3-bit 二進位常數；`3'b101` = 3-bit 二進位常數；`4'hF` = 4-bit
// [逐行教學 L062][語法] 十六進位常數；`==` 是相等比較，結果為 1 或 0。
// [逐行教學 L062][用途] 檢查 `sel==3'b100` 以決定資料/狀態要走哪一條路徑；檢查 `sel==3'b101` 以決定資料/狀態要走哪一條路徑；把 `else` 子模組放進目前模組並與內部訊號連線；更新
// [逐行教學 L062][用途] `digit`（目前七段顯示數字），下一值由 `timer/10` 計算；更新 `digit`（目前七段顯示數字），下一值由 `timer-(timer/10)*10` 計算；更新
// [逐行教學 L062][用途] `digit`（目前七段顯示數字），下一值由 `4'hF` 計算。
  if(sel==3'b100) digit=timer/10; else if(sel==3'b101) digit=timer-(timer/10)*10; else digit=4'hF;
// [逐行教學 L063][語法] `end` 結束最近一個 `begin` 區塊。
// [逐行教學 L063][用途] 關閉目前條件、時序或組合邏輯區塊。
 end
// [逐行教學 L064][語法] `bcd_to_seg7 UD(...)` 是子模組實例化；`UD` 是這一顆硬體實例名稱；`.port(signal)` 是具名 port mapping。
// [逐行教學 L064][用途] 七段顯示器解碼器：把 4-bit 數字/代碼轉成 a~g 七段亮滅資料。。
 bcd_to_seg7 UD(.bcd_in(digit),.seg7(seg7));
// [逐行教學 L065][語法] `endmodule` 與前面的 `module` 配對，結束模組定義。
// [逐行教學 L065][用途] 結束 `red_dot_top` 的 RTL 描述。
endmodule
