// [逐行教學 HEADER] ===============================================================
// [逐行教學 HEADER] 檔案：color_matrix_top.v
// [逐行教學 HEADER] 主要模組：color_matrix_top
// [逐行教學 HEADER] 整體用途：8×8 雙色 LED Matrix 頂層：整合文字 ROM、水平位移與列掃描。
// [逐行教學 HEADER] 每一個「原始實體行」前都加入 [語法] + [用途]，原始程式行本身不修改。
// [逐行教學 HEADER] 若一個原始行塞了多個敘述，註解會把同一行內的宣告/賦值/子模組一起拆解說明。
// [逐行教學 HEADER] ===============================================================
// [逐行教學 L001][語法] `module` 宣告一個硬體模組；模組名稱後括號列出對外 I/O。
// [逐行教學 L001][用途] 8×8 雙色 LED Matrix 頂層：整合文字 ROM、水平位移與列掃描。。
module color_matrix_top(clk, rst, sel, row, column_green, column_red); // 拿掉 .v，整理參數順序
// [逐行教學 L002][語法] `input` 宣告由外部進入模組的輸入埠。
// [逐行教學 L002][用途] 本段宣告：clk=系統時脈；rst=重置。
    input clk, rst;
// [逐行教學 L003][語法] `input` 宣告由外部進入模組的輸入埠；`[1:0]` 指定位元範圍。
// [逐行教學 L003][用途] 本段宣告：sel=掃描/選擇訊號。
    input [1:0] sel;    // 選擇LDE亮紅燈OR綠燈 (pin AA15, AA14)
// [逐行教學 L004][語法] `output` 宣告由模組送到外部的輸出埠；`[7:0]` 指定位元範圍。
// [逐行教學 L004][用途] 本段宣告：row=LED Matrix 列選；column_green=綠色欄像素；column_red=紅色欄像素。
    output [7:0] row, column_green, column_red; // column_green/red 比照Lab1
// [逐行教學 L005][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L005][用途] 把不同功能區塊視覺上分開，方便閱讀。
    
// [逐行教學 L006][語法] `wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動。
// [逐行教學 L006][用途] 本段宣告：clk_shift, clk_scan。
    wire clk_shift, clk_scan;
// [逐行教學 L007][語法] `wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[6:0]` 指定位元範圍。
// [逐行教學 L007][用途] 本段宣告：idx=位移索引。
    wire [6:0] idx, idx_cnt;
// [逐行教學 L008][語法] `wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[7:0]` 指定位元範圍。
// [逐行教學 L008][用途] 本段宣告：column_out。
    wire [7:0] column_out;
// [逐行教學 L009][語法] `wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[2:0]` 指定位元範圍。
// [逐行教學 L009][用途] 本段宣告：row_idx=目前掃描列索引。
    wire [2:0] row_idx;
// [逐行教學 L010][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L010][用途] 把不同功能區塊視覺上分開，方便閱讀。

// [逐行教學 L011][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L011][用途] 把不同功能區塊視覺上分開，方便閱讀。

// [逐行教學 L012][語法] `wire` 宣告連線型訊號，通常由 `assign` 或子模組輸出驅動；`[7:0]` 指定位元範圍。
// [逐行教學 L012][用途] 本段宣告：column_out_flip。
    wire [7:0] column_out_flip; // 宣告一條左右反轉後的共用電線
// [逐行教學 L013][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L013][用途] 把不同功能區塊視覺上分開，方便閱讀。
    
// [逐行教學 L014][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L014][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
    // 這裡把從 ROM 讀出來的 column_out 做左右位元對調
// [逐行教學 L015][語法] `assign 左式 = 右式;` 是 continuous assignment；這裡的 `=` 屬連續指定，不是 always 內的阻塞賦值；`{...}` 是 concatenation
// [逐行教學 L015][語法] 位元串接，可把多段 bit 合併/重排；`[msb:lsb]`/`[index]` 用來選取向量的一段或單一 bit。
// [逐行教學 L015][用途] 更新 `column_out_flip`，右式用 `{...}` 串接/重排多段位元；右式一變化就立即重新驅動左側 wire。
    assign column_out_flip = {column_out[0], column_out[1], column_out[2], column_out[3], column_out[4], column_out[5], column_out[6], column_out[7]};
// [逐行教學 L016][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L016][用途] 把不同功能區塊視覺上分開，方便閱讀。

// [逐行教學 L017][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L017][用途] 把不同功能區塊視覺上分開，方便閱讀。
    
// [逐行教學 L018][語法] `assign 左式 = 右式;` 是 continuous assignment；這裡的 `=` 屬連續指定，不是 always 內的阻塞賦值；`2'b01` = 2-bit
// [逐行教學 L018][語法] 二進位常數；`2'b11` = 2-bit 二進位常數；`8'b0` = 8-bit 二進位常數；`==` 是相等比較，結果為 1 或 0；`||` 是邏輯 OR：任一條件成立即為真；`條件 ? A
// [逐行教學 L018][語法] : B` 是三元條件運算子，硬體上等效於 2-to-1 MUX。
// [逐行教學 L018][用途] 更新 `column_green`（綠色欄像素），右式用三元運算依條件選一個來源（MUX）；右式一變化就立即重新驅動左側 wire。
    assign column_green = (sel == 2'b01 || sel == 2'b11) ? column_out_flip : 8'b0;
// [逐行教學 L019][語法] `assign 左式 = 右式;` 是 continuous assignment；這裡的 `=` 屬連續指定，不是 always 內的阻塞賦值；`2'b10` = 2-bit
// [逐行教學 L019][語法] 二進位常數；`2'b11` = 2-bit 二進位常數；`8'b0` = 8-bit 二進位常數；`==` 是相等比較，結果為 1 或 0；`||` 是邏輯 OR：任一條件成立即為真；`條件 ? A
// [逐行教學 L019][語法] : B` 是三元條件運算子，硬體上等效於 2-to-1 MUX。
// [逐行教學 L019][用途] 更新 `column_red`（紅色欄像素），右式用三元運算依條件選一個來源（MUX）；右式一變化就立即重新驅動左側 wire。
    assign column_red   = (sel == 2'b10 || sel == 2'b11) ? column_out_flip : 8'b0;
// [逐行教學 L020][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L020][用途] 把不同功能區塊視覺上分開，方便閱讀。
    
// [逐行教學 L021][語法] `//` 是 Verilog 單行註解；從 `//` 到行尾不參與模擬邏輯或硬體綜合。
// [逐行教學 L021][用途] 保留原作者對題目、接線、狀態或設計意圖的文字說明。
    // 將內部訊號 (wire) 連接到各個子模組 (Port Mapping)
// [逐行教學 L022][語法] `freq_div M1(...)` 是子模組實例化；`M1` 是這一顆硬體實例名稱；`#(22)` 是 parameter override，用新值取代子模組預設參數。
// [逐行教學 L022][用途] 時脈分頻器：利用二進位計數器的某一位，把 FPGA 高速系統時脈降成較慢時脈。。
    freq_div #(22) M1 (
// [逐行教學 L023][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L023][用途] 把子模組 `clk_in` 腳接到上層 `clk`。
        .clk_in(clk), 
// [逐行教學 L024][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L024][用途] 把子模組 `reset` 腳接到上層 `rst`。
        .reset(rst), 
// [逐行教學 L025][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L025][用途] 把子模組 `clk_out` 腳接到上層 `clk_shift`。
        .clk_out(clk_shift)
// [逐行教學 L026][語法] 這是一行 Verilog RTL 敘述；它延續目前區塊的宣告、控制或資料處理。
// [逐行教學 L026][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
    );
// [逐行教學 L027][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L027][用途] 把不同功能區塊視覺上分開，方便閱讀。
    
// [逐行教學 L028][語法] `freq_div M2(...)` 是子模組實例化；`M2` 是這一顆硬體實例名稱；`#(12)` 是 parameter override，用新值取代子模組預設參數。
// [逐行教學 L028][用途] 時脈分頻器：利用二進位計數器的某一位，把 FPGA 高速系統時脈降成較慢時脈。。
    freq_div #(12) M2 (
// [逐行教學 L029][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L029][用途] 把子模組 `clk_in` 腳接到上層 `clk`。
        .clk_in(clk), 
// [逐行教學 L030][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L030][用途] 把子模組 `reset` 腳接到上層 `rst`。
        .reset(rst), 
// [逐行教學 L031][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L031][用途] 把子模組 `clk_out` 腳接到上層 `clk_scan`。
        .clk_out(clk_scan)
// [逐行教學 L032][語法] 這是一行 Verilog RTL 敘述；它延續目前區塊的宣告、控制或資料處理。
// [逐行教學 L032][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
    );
// [逐行教學 L033][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L033][用途] 把不同功能區塊視覺上分開，方便閱讀。
    
// [逐行教學 L034][語法] `idx_gen M3(...)` 是子模組實例化；`M3` 是這一顆硬體實例名稱。
// [逐行教學 L034][用途] 水平位移索引產生器。。
    idx_gen M3 (
// [逐行教學 L035][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L035][用途] 把子模組 `clk` 腳接到上層 `clk_shift`。
        .clk(clk_shift), 
// [逐行教學 L036][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L036][用途] 把子模組 `rst` 腳接到上層 `rst`。
        .rst(rst), 
// [逐行教學 L037][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L037][用途] 把子模組 `idx` 腳接到上層 `idx`。
        .idx(idx)
// [逐行教學 L038][語法] 這是一行 Verilog RTL 敘述；它延續目前區塊的宣告、控制或資料處理。
// [逐行教學 L038][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
    ); 
// [逐行教學 L039][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L039][用途] 把不同功能區塊視覺上分開，方便閱讀。
    
// [逐行教學 L040][語法] `row_gen M4(...)` 是子模組實例化；`M4` 是這一顆硬體實例名稱。
// [逐行教學 L040][用途] LED Matrix 列掃描器：產生 row_idx 與 one-hot row。。
    row_gen M4 (
// [逐行教學 L041][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L041][用途] 把子模組 `clk` 腳接到上層 `clk_scan`。
        .clk(clk_scan), 
// [逐行教學 L042][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L042][用途] 把子模組 `rst` 腳接到上層 `rst`。
        .rst(rst), 
// [逐行教學 L043][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L043][用途] 把不同功能區塊視覺上分開，方便閱讀。
       
// [逐行教學 L044][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L044][用途] 把子模組 `row` 腳接到上層 `row`。
        .row(row), 
// [逐行教學 L045][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L045][用途] 把子模組 `row_idx` 腳接到上層 `row_idx`。
        .row_idx(row_idx)
// [逐行教學 L046][語法] 這是一行 Verilog RTL 敘述；它延續目前區塊的宣告、控制或資料處理。
// [逐行教學 L046][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
    );
// [逐行教學 L047][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L047][用途] 把不同功能區塊視覺上分開，方便閱讀。
    
// [逐行教學 L048][語法] `rom_char M5(...)` 是子模組實例化；`M5` 是這一顆硬體實例名稱。
// [逐行教學 L048][用途] 字型 ROM：依 row_idx 與 offset 取出目前 8×8 視窗像素。。
    rom_char M5 (
// [逐行教學 L049][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L049][用途] 把子模組 `row_idx` 腳接到上層 `row_idx`。
        .row_idx(row_idx),
// [逐行教學 L050][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L050][用途] 把子模組 `offset` 腳接到上層 `idx`。
        .offset(idx), 
// [逐行教學 L051][語法] `.port(signal)` 是具名埠對接，不依靠埠排列順序。
// [逐行教學 L051][用途] 把子模組 `data` 腳接到上層 `column_out`。
        .data(column_out)
// [逐行教學 L052][語法] 這是一行 Verilog RTL 敘述；它延續目前區塊的宣告、控制或資料處理。
// [逐行教學 L052][用途] 完成目前模組資料路徑或控制流程中的一個步驟。
    );
// [逐行教學 L053][語法] 空白行沒有 Verilog 語法作用；它只負責排版與區隔程式區塊。
// [逐行教學 L053][用途] 把不同功能區塊視覺上分開，方便閱讀。
    
// [逐行教學 L054][語法] `endmodule` 與前面的 `module` 配對，結束模組定義。
// [逐行教學 L054][用途] 結束 `color_matrix_top` 的 RTL 描述。
endmodule